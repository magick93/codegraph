//! Codelist enum for PayrollPaySlipPaymentMethod.
//! Generated by hr-graph. DO NOT EDIT.

use serde::{Deserialize, Serialize};
use utoipa::ToSchema;

/// Inline enum values for PaySlipType.paymentMethod
#[derive(Debug, Clone, Default, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
#[allow(clippy::enum_variant_names)]
pub enum PayrollPaySlipPaymentMethod {

    #[default]
    Electronically,

    Cheque,

    Manual,

}

impl std::fmt::Display for PayrollPaySlipPaymentMethod {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {

            Self::Electronically => write!(f, "Electronically"),

            Self::Cheque => write!(f, "Cheque"),

            Self::Manual => write!(f, "Manual"),

        }
    }
}

impl std::str::FromStr for PayrollPaySlipPaymentMethod {
    type Err = String;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s {

            "Electronically" => Ok(Self::Electronically),

            "Cheque" => Ok(Self::Cheque),

            "Manual" => Ok(Self::Manual),

            _ => Err(format!("unknown PayrollPaySlipPaymentMethod value: {}", s)),
        }
    }
}
