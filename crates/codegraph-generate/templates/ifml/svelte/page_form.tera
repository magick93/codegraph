<script lang="ts">
	import type { PageData } from './$types';

	let { data }: { data: PageData } = $props();
</script>

<svelte:head>
	<title>{{ label }}</title>
</svelte:head>

<form method="POST">
	{% for field in fields %}
	<label>
		{{ field }}
		<input name="{{ field }}" />
	</label>
	{% endfor %}
	<div class="actions">
		<button type="submit">Save</button>
		<button type="button" onclick={% raw %}history.back(){% endraw %}>Cancel</button>
	</div>
</form>
