<script lang="ts">
	import { EntityDetail, createEntityApi } from '@crewbase/entities';
	import { {{ entity_name }}Descriptor } from '../../descriptor';
	import { apiClient } from '$lib/api/client';

	let { data } = $props();

	const api = createEntityApi({{ entity_name }}Descriptor, { fetch, baseUrl: apiClient.baseUrl });
</script>

<EntityDetail descriptor={{ "{" }}{{ entity_name }}Descriptor{{ "}" }} id={data.id} {api} />
