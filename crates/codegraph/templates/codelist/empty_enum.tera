//! Codelist enum for {{ enum_name }}.
//! Generated by {{ project.generator_name }}. DO NOT EDIT.
//!
//! This codelist has no defined values in the BIAN data source.
//! The empty enum stub exists so that DTO code referencing
//! `crate::codelist::{{ enum_name }}` compiles.

use serde::{Deserialize, Serialize};
use utoipa::ToSchema;

{% if description %}/// {{ description }}
{% else %}/// {{ enum_name }} codelist values (empty — no values defined in BIAN data).
{% endif -%}
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, ToSchema)]
pub enum {{ enum_name }} {}

impl Default for {{ enum_name }} {
    fn default() -> Self {
        unreachable!("{{ enum_name }} has no defined values in BIAN data")
    }
}

impl std::fmt::Display for {{ enum_name }} {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match *self {}
    }
}

impl std::str::FromStr for {{ enum_name }} {
    type Err = String;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        Err(format!("{{ enum_name }} has no defined values in BIAN data; cannot parse: {}", s))
    }
}
