// Generated by codegraph. DO NOT EDIT.
// Typed API client for {{ entity_name }}

import type { APIRequestContext } from '@playwright/test';

export class {{ entity_name }}Api {
  constructor(private request: APIRequestContext, private baseUrl: string = '') {}

{% if has_create %}
  async create(data: any): Promise<{ status: number; body: any }> {
    const response = await this.request.post(`${this.baseUrl}/api/{{ project.api_version }}/{{ domain }}/{{ path_segment }}`, { data });
    return { status: response.status(), body: await response.json().catch(() => ({})) };
  }
{% endif %}
{% if has_read %}
  async get(id: string): Promise<{ status: number; body: any }> {
    const response = await this.request.get(`${this.baseUrl}/api/{{ project.api_version }}/{{ domain }}/{{ path_segment }}/${id}`);
    return { status: response.status(), body: await response.json().catch(() => ({})) };
  }
{% endif %}
{% if has_list %}
  async list(params?: Record<string, string>): Promise<{ status: number; body: any }> {
    const qs = params ? '?' + new URLSearchParams(params).toString() : '';
    const response = await this.request.get(`${this.baseUrl}/api/{{ project.api_version }}/{{ domain }}/{{ path_segment }}${qs}`);
    return { status: response.status(), body: await response.json().catch(() => ({})) };
  }
{% endif %}
{% if has_update %}
  async update(id: string, data: any): Promise<{ status: number; body: any }> {
    const response = await this.request.patch(`${this.baseUrl}/api/{{ project.api_version }}/{{ domain }}/{{ path_segment }}/${id}`, { data });
    return { status: response.status(), body: await response.json().catch(() => ({})) };
  }
{% endif %}
{% if has_delete %}
  async delete(id: string): Promise<{ status: number }> {
    const response = await this.request.delete(`${this.baseUrl}/api/{{ project.api_version }}/{{ domain }}/{{ path_segment }}/${id}`);
    return { status: response.status() };
  }
{% endif %}
}
