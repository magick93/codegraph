// Generated by codegraph. DO NOT EDIT.
// Test data fixtures for {{ entity_name }}

export interface Create{{ entity_name }}Dto {
{% for field in create_fields %}
  {{ field.name }}{% if not field.required %}?{% endif %}: {{ field.ts_type }};
{% endfor %}
}

export class {{ entity_name }}Fixture {
  static valid(overrides: Partial<Create{{ entity_name }}Dto> = {}): Create{{ entity_name }}Dto {
    return {
{% for field in create_fields %}
{% if field.required %}
      {{ field.name }}: {{ field.example_value }},
{% endif %}
{% endfor %}
      ...overrides,
    } as Create{{ entity_name }}Dto;
  }

  static missingRequired(): Record<string, unknown> {
    // Returns an empty object — missing all required fields, guaranteed 422
    return {};
  }

  static withOptionalFields(overrides: Partial<Create{{ entity_name }}Dto>): Create{{ entity_name }}Dto {
    return { ...this.valid(), ...overrides };
  }
{% if fk_fields %}
{% for field in fk_fields %}
  static withParent{{ field.entity_name }}(parentId: string): Create{{ entity_name }}Dto {
    return { ...this.valid(), {{ field.name }}: parentId };
  }
{% endfor %}
{% endif %}
}
