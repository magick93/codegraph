import { pluginSettingsSchema, type SettingField } from "@community-os/emdash-common";

/**
 * {{ p.plugin_label }} plugin settings beyond the shared gateway credentials.
 *
 * GENERATED by codegraph (emdash_plugin) — do not edit by hand.
 */
export function {{ p.domain_kebab }}SettingsSchema(): Record<string, SettingField> {
  return pluginSettingsSchema({
{%- for s in p.settings %}
    {{ s.key }}: {
      type: "{{ s.type }}",
      label: "{{ s.label }}",
{%- if s.help %}
      description: "{{ s.help }}",
{%- endif %}
{%- if s.default %}
      default: {{ s.default }},
{%- endif %}
    },
{%- endfor %}
  });
}
