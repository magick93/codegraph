{% if project.hooks_api_crate %}
//! Hook registry for the {{ name }} worker.
//! Generated by {{ project.generator_name }}. DO NOT EDIT.

// The registry itself is shared: the hooks-api crate owns a single registry
// of lifecycle traits for all domains, and each worker re-exports it. The
// per-domain lifecycle traits remain compiled in the hooks-api crate
// (`{{ project.hooks_api_crate }}::generated::{domain}::{module}`), which is
// what the routed command/query code references.
pub use {{ project.hooks_api_crate }}::generated::registry::HookRegistry;
{% endif %}
