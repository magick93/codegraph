# Generated by {{ project.generator_name }}. DO NOT EDIT.
# Cargo workspace for the {{ app_name }} multi-worker deployment (Workers topology).
# One crate per domain under workers/{domain}/ plus the gateway.

[workspace]
members = [
{% for domain in domains %}
    "{{ domain.name }}",
{% endfor %}
    "gateway",
]

[workspace.dependencies]
axum = { version = "0.8", features = ["json", "multipart"] }
{% if project.database_target == "sqlite" %}
sea-orm = { version = "1", features = ["sqlx-sqlite", "runtime-tokio-rustls"] }
{% else %}
sea-orm = { version = "1", features = ["sqlx-postgres", "runtime-tokio-rustls"] }
{% endif %}
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_qs = "0.13"
percent-encoding = "2"
uuid = { version = "1", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
utoipa = { version = "5", features = ["axum_extras", "chrono", "uuid", "decimal"] }
garde = { version = "0.22", features = ["derive"] }
rust_decimal = { version = "1", features = ["serde-with-str"] }
jsonwebtoken = "9"
async-trait = "0.1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3" }
tower-service = "0.3"
http-body-util = "0.1"
worker = { version = "0.8", default-features = false }
{% if project.codegraph_workflow_base %}codegraph-workflow = { path = "{{ project.codegraph_workflow_base }}" }
{% else %}codegraph-workflow = { git = "https://github.com/magick93/codegraph.git", rev = "{{ project.codegraph_rev }}" }{% endif %}
{% if project.type_contracts_base %}codegraph-type-contracts = { path = "{{ project.type_contracts_base }}" }
{% else %}codegraph-type-contracts = { git = "https://github.com/magick93/codegraph.git", rev = "{{ project.codegraph_rev }}" }{% endif %}
{% if project.persistence_provider == "cornucopia" %}
tokio-postgres = "0.7"
deadpool-postgres = "0.14"
{% endif %}
