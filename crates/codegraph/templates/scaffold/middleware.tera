//! Authentication middleware — supports JWT (Supabase) and API key auth.
//! Generated by {{ project.generator_name }}. DO NOT EDIT.
//!
//! The `AuthInfo` / `AuthMode` vocabulary and the `auth_middleware` / API-key /
//! JWT verification logic live in `cosmos-extensions::http::middleware`
//! (hand-written crate); this generated module re-exports the shared types the
//! generated handlers and permission middleware consume. The generated server
//! wires `cosmos_extensions::http::middleware::auth_middleware` onto the
//! `/api/v1` stack via a closure capturing `Arc<AppContext>` (server.tera).

pub mod permission;

pub use cosmos_extensions::http::middleware::{ApiKeyInfo, AuthInfo, AuthMode};
