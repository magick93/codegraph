//! CLI commands for rsvp domain (RSVP).
//! Generated by codegraph. DO NOT EDIT.

use clap::Subcommand;

use crate::client::ApiClient;
use crate::output::OutputFormat;

pub mod rsvp;


#[derive(Subcommand)]
pub enum RsvpCommand {

    /// Manage rsvps
    Rsvp {
        #[command(subcommand)]
        command: rsvp::RsvpCommand,
    },

}

pub async fn execute(
    cmd: RsvpCommand,
    client: &ApiClient,
    format: OutputFormat,
) -> anyhow::Result<()> {
    match cmd {

        RsvpCommand::Rsvp { command } => {
            rsvp::execute(command, client, format).await
        }

    }
}
