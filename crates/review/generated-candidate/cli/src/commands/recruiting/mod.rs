//! CLI commands for recruiting domain (Recruiting).
//! Generated by hr-graph. DO NOT EDIT.

use clap::Subcommand;

use crate::client::ApiClient;
use crate::output::OutputFormat;

pub mod application;

pub mod candidate;


#[derive(Subcommand)]
pub enum RecruitingCommand {

    /// Manage applications
    Application {
        #[command(subcommand)]
        command: application::ApplicationCommand,
    },

    /// Manage candidates
    Candidate {
        #[command(subcommand)]
        command: candidate::CandidateCommand,
    },

}

pub async fn execute(
    cmd: RecruitingCommand,
    client: &ApiClient,
    format: OutputFormat,
) -> anyhow::Result<()> {
    match cmd {

        RecruitingCommand::Application { command } => {
            application::execute(command, client, format).await
        }

        RecruitingCommand::Candidate { command } => {
            candidate::execute(command, client, format).await
        }

    }
}
