//! CLI commands for Application in recruiting domain.
//! Generated by hr-graph. DO NOT EDIT.

use clap::{Args, Subcommand};
use uuid::Uuid;

use crate::client::ApiClient;
use crate::output::OutputFormat;
use crate::util::{parse_key_value, resolve_body, resolve_body_with_sets};

#[derive(Subcommand)]
pub enum ApplicationCommand {

    /// List applications
    List(ApplicationListArgs),


    /// Get a application by ID
    Get {
        /// The application ID
        id: Uuid,
    },


    /// Create a new application
    Create(ApplicationCreateArgs),
    /// Bulk-create applications from a JSON array
    BulkCreate(ApplicationBulkCreateArgs),


    /// Update a application
    Update {
        /// The application ID
        id: Uuid,
        #[command(flatten)]
        args: ApplicationUpdateArgs,
    },


    /// Delete a application
    Delete {
        /// The application ID
        id: Uuid,
    },



}


#[derive(Args)]
pub struct ApplicationListArgs {
    /// Page number
    #[arg(long, default_value_t = 0)]
    pub page: u64,

    /// Results per page
    #[arg(long, default_value_t = 20)]
    pub page_size: u64,

    /// Filter results (repeatable). Allowed fields: application_id, status

    #[arg(long = "filter", value_parser = parse_key_value)]
    pub filters: Vec<(String, String)>,
}



#[derive(Args)]
pub struct ApplicationCreateArgs {
    /// Create from a JSON file
    #[arg(long)]
    pub from_file: Option<std::path::PathBuf>,

    /// Create from inline JSON
    #[arg(long)]
    pub json: Option<String>,

}

#[derive(Args)]
pub struct ApplicationBulkCreateArgs {
    /// Create from a JSON file containing an array of items
    #[arg(long)]
    pub from_file: Option<std::path::PathBuf>,

    /// Create from inline JSON array
    #[arg(long)]
    pub json: Option<String>,
}



#[derive(Args)]
pub struct ApplicationUpdateArgs {
    /// Update from a JSON file
    #[arg(long)]
    pub from_file: Option<std::path::PathBuf>,

    /// Update from inline JSON
    #[arg(long)]
    pub json: Option<String>,

    /// Set individual fields as key=value pairs
    #[arg(long = "set", value_parser = parse_key_value)]
    pub fields: Vec<(String, String)>,
}


pub async fn execute(
    cmd: ApplicationCommand,
    client: &ApiClient,
    format: OutputFormat,
) -> anyhow::Result<()> {
    let base = format!("/api/recruiting/application");

    match cmd {

        ApplicationCommand::List(args) => {
            let mut url = format!(
                "{}?page={}&page_size={}",
                base, args.page, args.page_size
            );
            for (key, value) in &args.filters {
                url.push_str(&format!(
                    "&filter[{}]={}",
                    urlencoding::encode(key),
                    urlencoding::encode(value),
                ));
            }
            let resp = client.get(&url).await?;
            format.print(&resp)?;
        }


        ApplicationCommand::Get { id } => {
            let url = format!("{base}/{id}");
            let resp = client.get(&url).await?;
            format.print(&resp)?;
        }


        ApplicationCommand::Create(args) => {
            let body = resolve_body(args.from_file, args.json, &[

            ])?;
            let resp = client.post(&base, &body).await?;
            format.print(&resp)?;
        }
        ApplicationCommand::BulkCreate(args) => {
            let body = resolve_body(args.from_file, args.json, &[])?;
            let resp = client.post_raw(&base, &body).await?;
            match resp.status().as_u16() {
                207 => {
                    let result: serde_json::Value = resp.json().await?;
                    let success_count = result["success"].as_array().map(|a| a.len()).unwrap_or(0);
                    let failed = result["failed"].as_array();
                    let failed_count = failed.map(|a| a.len()).unwrap_or(0);
                    println!("Bulk create: {} succeeded, {} failed", success_count, failed_count);
                    if let Some(failures) = failed {
                        for f in failures {
                            eprintln!("  [{}] {}", f["index"], f["error"]);
                        }
                    }
                    format.print(&result)?;
                }
                201 => {
                    let result: serde_json::Value = resp.json().await?;
                    println!("Created 1 item (single-item response)");
                    format.print(&result)?;
                }
                status => {
                    let body = resp.text().await?;
                    anyhow::bail!("Bulk create failed with status {status}: {body}");
                }
            }
        }


        ApplicationCommand::Update { id, args } => {
            let body = resolve_body_with_sets(args.from_file, args.json, &args.fields)?;
            let url = format!("{base}/{id}");
            let resp = client.put(&url, &body).await?;
            format.print(&resp)?;
        }


        ApplicationCommand::Delete { id } => {
            let url = format!("{base}/{id}");
            client.delete(&url).await?;
            println!("Deleted application {id}");
        }



    }

    Ok(())
}
