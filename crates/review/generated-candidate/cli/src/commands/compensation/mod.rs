//! CLI commands for compensation domain (Compensation).
//! Generated by hr-graph. DO NOT EDIT.

use clap::Subcommand;

use crate::client::ApiClient;
use crate::output::OutputFormat;

pub mod pay_run;


#[derive(Subcommand)]
pub enum CompensationCommand {

    /// Manage pay_runs
    PayRun {
        #[command(subcommand)]
        command: pay_run::PayRunCommand,
    },

}

pub async fn execute(
    cmd: CompensationCommand,
    client: &ApiClient,
    format: OutputFormat,
) -> anyhow::Result<()> {
    match cmd {

        CompensationCommand::PayRun { command } => {
            pay_run::execute(command, client, format).await
        }

    }
}
