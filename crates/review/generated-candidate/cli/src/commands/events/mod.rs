//! CLI commands for events domain (Events).
//! Generated by codegraph. DO NOT EDIT.

use clap::Subcommand;

use crate::client::ApiClient;
use crate::output::OutputFormat;

pub mod public_event;


#[derive(Subcommand)]
pub enum EventsCommand {

    /// Manage public_events
    PublicEvent {
        #[command(subcommand)]
        command: public_event::PublicEventCommand,
    },

}

pub async fn execute(
    cmd: EventsCommand,
    client: &ApiClient,
    format: OutputFormat,
) -> anyhow::Result<()> {
    match cmd {

        EventsCommand::PublicEvent { command } => {
            public_event::execute(command, client, format).await
        }

    }
}
