// Generated by codegraph. DO NOT EDIT.
// E2E tests for Rsvp

import { test, expect } from '@playwright/test';
import { RsvpFixture } from '../../fixtures/rsvp/rsvp_rsvp';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('Rsvp — Create', () => {
  test('POST /api/v1/rsvp/rsvp creates a Rsvp', async ({ request }) => {
    const payload = RsvpFixture.valid();
    const response = await request.post('/api/v1/rsvp/rsvp', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');





    expect(data.status).toBe(payload.status);





    expect(data.timestamp).toEqual(payload.timestamp);



  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = RsvpFixture.valid();
    const response = await request.post('/api/v1/rsvp/rsvp', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = RsvpFixture.missingRequired();
    const response = await request.post('/api/v1/rsvp/rsvp', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('Rsvp — Read', () => {
  test('GET /api/v1/rsvp/rsvp returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/rsvp/rsvp', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/rsvp/rsvp/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/rsvp/rsvp/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(404);
  });
});



test.describe('Rsvp — Delete', () => {
  test('DELETE /api/v1/rsvp/rsvp/:id removes a Rsvp', async ({ request }) => {
    const created = await request.post('/api/v1/rsvp/rsvp', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: RsvpFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/rsvp/rsvp/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/rsvp/rsvp/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(check.status()).toBe(404);
  });
});



test.describe('Rsvp — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/rsvp/rsvp?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

