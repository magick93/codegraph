// Generated by codegraph. DO NOT EDIT.
// E2E tests for PublicEvent

import { test, expect } from '@playwright/test';
import { PublicEventFixture } from '../../fixtures/events/events_public_event';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('PublicEvent — Create', () => {
  test('POST /api/v1/events/public-event creates a PublicEvent', async ({ request }) => {
    const payload = PublicEventFixture.valid();
    const response = await request.post('/api/v1/events/public-event', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');



    expect(data.familyName).toBe(payload.familyName);





    expect(data.givenName).toBe(payload.givenName);





    expect(data.title).toBe(payload.title);









  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = PublicEventFixture.valid();
    const response = await request.post('/api/v1/events/public-event', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = PublicEventFixture.missingRequired();
    const response = await request.post('/api/v1/events/public-event', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('PublicEvent — Read', () => {
  test('GET /api/v1/events/public-event returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/events/public-event', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/events/public-event/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/events/public-event/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(response.status()).toBe(404);
  });
});



test.describe('PublicEvent — Delete', () => {
  test('DELETE /api/v1/events/public-event/:id removes a PublicEvent', async ({ request }) => {
    const created = await request.post('/api/v1/events/public-event', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: PublicEventFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/events/public-event/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/events/public-event/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(check.status()).toBe(404);
  });
});



test.describe('PublicEvent — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/events/public-event?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

