// Generated by codegraph. DO NOT EDIT.
// E2E tests for DistributionBase

import { test, expect } from '@playwright/test';
import { DistributionBaseFixture } from '../../fixtures/common/common_distribution_base';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('DistributionBase — Create', () => {
  test('POST /api/v1/common/distribution-base creates a DistributionBase', async ({ request }) => {
    const payload = DistributionBaseFixture.valid();
    const response = await request.post('/api/v1/common/distribution-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');



    expect(data.startDate).toEqual(payload.startDate);







  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = DistributionBaseFixture.valid();
    const response = await request.post('/api/v1/common/distribution-base', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = DistributionBaseFixture.missingRequired();
    const response = await request.post('/api/v1/common/distribution-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('DistributionBase — Read', () => {
  test('GET /api/v1/common/distribution-base returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/distribution-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/distribution-base/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/distribution-base/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(404);
  });
});



test.describe('DistributionBase — Delete', () => {
  test('DELETE /api/v1/common/distribution-base/:id removes a DistributionBase', async ({ request }) => {
    const created = await request.post('/api/v1/common/distribution-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: DistributionBaseFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/distribution-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/distribution-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(check.status()).toBe(404);
  });
});



test.describe('DistributionBase — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/distribution-base?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

