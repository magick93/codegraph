// Generated by codegraph. DO NOT EDIT.
// E2E tests for FormattedDateTime

import { test, expect } from '@playwright/test';
import { FormattedDateTimeFixture } from '../../fixtures/common/common_formatted_date_time';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('FormattedDateTime — Create', () => {
  test('POST /api/v1/common/formatted-date-time creates a FormattedDateTime', async ({ request }) => {
    const payload = FormattedDateTimeFixture.valid();
    const response = await request.post('/api/v1/common/formatted-date-time', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');

  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = FormattedDateTimeFixture.valid();
    const response = await request.post('/api/v1/common/formatted-date-time', { data });
    expect(response.status()).toBe(401);
  });


});



test.describe('FormattedDateTime — Read', () => {
  test('GET /api/v1/common/formatted-date-time returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/formatted-date-time', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/formatted-date-time/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/formatted-date-time/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(404);
  });
});



test.describe('FormattedDateTime — Delete', () => {
  test('DELETE /api/v1/common/formatted-date-time/:id removes a FormattedDateTime', async ({ request }) => {
    const created = await request.post('/api/v1/common/formatted-date-time', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: FormattedDateTimeFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/formatted-date-time/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/formatted-date-time/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(check.status()).toBe(404);
  });
});



test.describe('FormattedDateTime — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/formatted-date-time?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

