// Generated by codegraph. DO NOT EDIT.
// E2E tests for PersonBase

import { test, expect } from '@playwright/test';
import { PersonBaseFixture } from '../../fixtures/common/common_person_base';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('PersonBase — Create', () => {
  test('POST /api/v1/common/person-base creates a PersonBase', async ({ request }) => {
    const payload = PersonBaseFixture.valid();
    const response = await request.post('/api/v1/common/person-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');



    expect(data.familyName).toBe(payload.familyName);





    expect(data.givenName).toBe(payload.givenName);





  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = PersonBaseFixture.valid();
    const response = await request.post('/api/v1/common/person-base', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = PersonBaseFixture.missingRequired();
    const response = await request.post('/api/v1/common/person-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('PersonBase — Read', () => {
  test('GET /api/v1/common/person-base returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/person-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/person-base/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/person-base/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(404);
  });
});



test.describe('PersonBase — Delete', () => {
  test('DELETE /api/v1/common/person-base/:id removes a PersonBase', async ({ request }) => {
    const created = await request.post('/api/v1/common/person-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: PersonBaseFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/person-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/person-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(check.status()).toBe(404);
  });
});



test.describe('PersonBase — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/person-base?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

