// Generated by codegraph. DO NOT EDIT.
// E2E tests for EventBase

import { test, expect } from '@playwright/test';
import { EventBaseFixture } from '../../fixtures/common/common_event_base';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('EventBase — Create', () => {
  test('POST /api/v1/common/event-base creates a EventBase', async ({ request }) => {
    const payload = EventBaseFixture.valid();
    const response = await request.post('/api/v1/common/event-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');



    expect(data.familyName).toBe(payload.familyName);





    expect(data.givenName).toBe(payload.givenName);





    expect(data.title).toBe(payload.title);







  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = EventBaseFixture.valid();
    const response = await request.post('/api/v1/common/event-base', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = EventBaseFixture.missingRequired();
    const response = await request.post('/api/v1/common/event-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('EventBase — Read', () => {
  test('GET /api/v1/common/event-base returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/event-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/event-base/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/event-base/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(404);
  });
});



test.describe('EventBase — Delete', () => {
  test('DELETE /api/v1/common/event-base/:id removes a EventBase', async ({ request }) => {
    const created = await request.post('/api/v1/common/event-base', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: EventBaseFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/event-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/event-base/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(check.status()).toBe(404);
  });
});



test.describe('EventBase — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/event-base?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

