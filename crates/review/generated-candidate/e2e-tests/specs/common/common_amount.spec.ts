// Generated by codegraph. DO NOT EDIT.
// E2E tests for Amount

import { test, expect } from '@playwright/test';
import { AmountFixture } from '../../fixtures/common/common_amount';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('Amount — Create', () => {
  test('POST /api/v1/common/amount creates a Amount', async ({ request }) => {
    const payload = AmountFixture.valid();
    const response = await request.post('/api/v1/common/amount', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');





  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = AmountFixture.valid();
    const response = await request.post('/api/v1/common/amount', { data });
    expect(response.status()).toBe(401);
  });


});



test.describe('Amount — Read', () => {
  test('GET /api/v1/common/amount returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/amount', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/amount/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/amount/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(response.status()).toBe(404);
  });
});



test.describe('Amount — Delete', () => {
  test('DELETE /api/v1/common/amount/:id removes a Amount', async ({ request }) => {
    const created = await request.post('/api/v1/common/amount', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: AmountFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/amount/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/amount/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(check.status()).toBe(404);
  });
});



test.describe('Amount — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/amount?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

