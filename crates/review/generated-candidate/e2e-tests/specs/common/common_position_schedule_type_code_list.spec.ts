// Generated by codegraph. DO NOT EDIT.
// E2E tests for PositionScheduleTypeCodeList

import { test, expect } from '@playwright/test';
import { PositionScheduleTypeCodeListFixture } from '../../fixtures/common/common_position_schedule_type_code_list';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('PositionScheduleTypeCodeList — Create', () => {
  test('POST /api/v1/common/position-schedule-type-code-list creates a PositionScheduleTypeCodeList', async ({ request }) => {
    const payload = PositionScheduleTypeCodeListFixture.valid();
    const response = await request.post('/api/v1/common/position-schedule-type-code-list', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');

  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = PositionScheduleTypeCodeListFixture.valid();
    const response = await request.post('/api/v1/common/position-schedule-type-code-list', { data });
    expect(response.status()).toBe(401);
  });


});



test.describe('PositionScheduleTypeCodeList — Read', () => {
  test('GET /api/v1/common/position-schedule-type-code-list returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/position-schedule-type-code-list', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/position-schedule-type-code-list/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/position-schedule-type-code-list/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(response.status()).toBe(404);
  });



test.describe('PositionScheduleTypeCodeList — Delete', () => {
  test('DELETE /api/v1/common/position-schedule-type-code-list/:id removes a PositionScheduleTypeCodeList', async ({ request }) => {
    const created = await request.post('/api/v1/common/position-schedule-type-code-list', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: PositionScheduleTypeCodeListFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/position-schedule-type-code-list/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/position-schedule-type-code-list/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(check.status()).toBe(404);
  });
});



test.describe('PositionScheduleTypeCodeList — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/position-schedule-type-code-list?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

