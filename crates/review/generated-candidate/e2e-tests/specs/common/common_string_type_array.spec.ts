// Generated by codegraph. DO NOT EDIT.
// E2E tests for StringTypeArray

import { test, expect } from '@playwright/test';
import { StringTypeArrayFixture } from '../../fixtures/common/common_string_type_array';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('StringTypeArray — Create', () => {
  test('POST /api/v1/common/string-type-array creates a StringTypeArray', async ({ request }) => {
    const payload = StringTypeArrayFixture.valid();
    const response = await request.post('/api/v1/common/string-type-array', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');

  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = StringTypeArrayFixture.valid();
    const response = await request.post('/api/v1/common/string-type-array', { data });
    expect(response.status()).toBe(401);
  });


});



test.describe('StringTypeArray — Read', () => {
  test('GET /api/v1/common/string-type-array returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/common/string-type-array', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/common/string-type-array/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/common/string-type-array/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(response.status()).toBe(404);
  });
});



test.describe('StringTypeArray — Delete', () => {
  test('DELETE /api/v1/common/string-type-array/:id removes a StringTypeArray', async ({ request }) => {
    const created = await request.post('/api/v1/common/string-type-array', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: StringTypeArrayFixture.valid(),
    });
    expect(created.status()).toBe(201);
    const { data } = await created.json();
    const id = data.id;

    const response = await request.delete(`/api/v1/common/string-type-array/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(204);

    const check = await request.get(`/api/v1/common/string-type-array/${id}`, {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(check.status()).toBe(404);
  });
});



test.describe('StringTypeArray — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/common/string-type-array?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

