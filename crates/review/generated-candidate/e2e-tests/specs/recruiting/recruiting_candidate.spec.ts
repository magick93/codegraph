// Generated by codegraph. DO NOT EDIT.
// E2E tests for Candidate

import { test, expect } from '@playwright/test';
import { CandidateFixture } from '../../fixtures/recruiting/recruiting_candidate';


const authToken = process.env.TEST_AUTH_TOKEN || '';


test.describe('Candidate — Create', () => {
  test('POST /api/v1/recruiting/candidate creates a Candidate', async ({ request }) => {
    const payload = CandidateFixture.valid();
    const response = await request.post('/api/v1/recruiting/candidate', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data: payload,
    });
    expect(response.status()).toBe(201);
    const { data } = await response.json();
    expect(data).toHaveProperty('id');



    expect(data.candidateId).toBe(payload.candidateId);





    expect(data.familyName).toBe(payload.familyName);





    expect(data.givenName).toBe(payload.givenName);





























  });

  test('POST without auth rejects with 401', async ({ request }) => {
    const data = CandidateFixture.valid();
    const response = await request.post('/api/v1/recruiting/candidate', { data });
    expect(response.status()).toBe(401);
  });


  test('POST with missing required fields rejects with 422', async ({ request }) => {
    const data = CandidateFixture.missingRequired();
    const response = await request.post('/api/v1/recruiting/candidate', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
      data,
    });
    expect(response.status()).toBe(422);
  });

});



test.describe('Candidate — Read', () => {
  test('GET /api/v1/recruiting/candidate returns paginated list', async ({ request }) => {
    const response = await request.get('/api/v1/recruiting/candidate', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body).toHaveProperty('data');
    expect(Array.isArray(body.data)).toBe(true);
  });

  test('GET /api/v1/recruiting/candidate/:id returns 404 for nonexistent', async ({ request }) => {
    const response = await request.get('/api/v1/recruiting/candidate/00000000-0000-0000-0000-000000000000', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
        expect(response.status()).toBe(404);
  });





test.describe('Candidate — Pagination', () => {
  test('list supports page_size', async ({ request }) => {
    const response = await request.get('/api/v1/recruiting/candidate?page=0&page_size=5', {
      headers: authToken ? { Authorization: `Bearer ${authToken}` } : {},
    });
    expect(response.status()).toBe(200);
    const body = await response.json();
    expect(body.data.length).toBeLessThanOrEqual(5);
  });
});

