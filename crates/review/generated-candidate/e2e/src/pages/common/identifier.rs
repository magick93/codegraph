// Generated by codegraph — DO NOT EDIT.
// Page Object Model for Identifier.

use playwright::api::Page;
use anyhow::Result;
use std::collections::HashMap;
use serde_json::Value;

pub struct IdentifierPage<'a> {
    pub page: &'a Page,
}

impl<'a> IdentifierPage<'a> {
    pub fn new(page: &'a Page) -> Self {
        Self { page }
    }

    fn base_url() -> String {
        std::env::var("PW_BASE_URL").unwrap_or_else(|_| "http://localhost:5173".to_string())
    }

    // ── Navigation ───────────────────────────────────────────────────────────

    pub async fn goto_list(&self) -> Result<()> {
        self.page.goto_builder(&format!("{}/common/identifier", Self::base_url())).goto().await?;
        Ok(())
    }


    pub async fn goto_create(&self) -> Result<()> {
        self.page.goto_builder(&format!("{}/common/identifier/new", Self::base_url())).goto().await?;
        Ok(())
    }



    pub async fn goto_detail(&self, id: &str) -> Result<()> {
        self.page
            .goto_builder(&format!("{}/common/identifier/{id}", Self::base_url()))
            .goto()
            .await?;
        Ok(())
    }


    // ── Field interactions (Component Object Model) ──────────────────────────



    pub async fn fill_scheme_agency_id(&self, value: &str) -> Result<()> {
        self.page.fill_builder("#scheme_agency_id", value).fill().await?;
        Ok(())
    }




    pub async fn fill_scheme_id(&self, value: &str) -> Result<()> {
        self.page.fill_builder("#scheme_id", value).fill().await?;
        Ok(())
    }




    pub async fn fill_scheme_version_id(&self, value: &str) -> Result<()> {
        self.page.fill_builder("#scheme_version_id", value).fill().await?;
        Ok(())
    }




    pub async fn fill_value(&self, value: &str) -> Result<()> {
        self.page.fill_builder("#value", value).fill().await?;
        Ok(())
    }




    // ── Actions ──────────────────────────────────────────────────────────────


    pub async fn submit(&self) -> Result<()> {
        self.page
            .click_builder("[data-testid=\"identifier-submit-btn\"]")
            .click()
            .await?;
        Ok(())
    }



    pub async fn delete(&self) -> Result<()> {
        self.page
            .click_builder("[data-testid=\"identifier-delete-btn\"]")
            .click()
            .await?;
        self.page
            .click_builder("[data-testid=\"confirm-dialog-confirm\"]")
            .click()
            .await?;
        Ok(())
    }





    // ── Convenience: create via form, return ID ──────────────────────────────

    /// Navigate to the create page, fill each field from `data`, submit,
    /// and extract the entity ID from the post-redirect URL.
    pub async fn create(&self, data: &HashMap<&str, Value>) -> Result<String> {
        self.goto_create().await?;


        if let Some(v) = data.get("scheme_agency_id").and_then(|v| v.as_str()) {
            self.fill_scheme_agency_id(v).await.ok();
        }



        if let Some(v) = data.get("scheme_id").and_then(|v| v.as_str()) {
            self.fill_scheme_id(v).await.ok();
        }



        if let Some(v) = data.get("scheme_version_id").and_then(|v| v.as_str()) {
            self.fill_scheme_version_id(v).await.ok();
        }



        if let Some(v) = data.get("value").and_then(|v| v.as_str()) {
            self.fill_value(v).await.ok();
        }


        self.submit().await?;
        let url: String = self.page.url()?;
        let id = url
            .split('/')
            .last()
            .unwrap_or("")
            .to_string();
        Ok(id)
    }

}
