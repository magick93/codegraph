//! Generated repository implementation for PublicEvent.
//! DO NOT EDIT — generated by codegraph.

use async_trait::async_trait;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseTransaction, EntityTrait, PaginatorTrait, QueryFilter, QueryOrder, Set,
};
use uuid::Uuid;

use super::repository::PublicEventRepository;
use super::dto_create::CreatePublicEventRequest;
use super::dto_update::UpdatePublicEventRequest;
use super::dto_response::PublicEventResponse;

pub struct PublicEventRepositoryImpl;

#[async_trait]
impl PublicEventRepository<sea_orm::DatabaseTransaction> for PublicEventRepositoryImpl {
    #[tracing::instrument(skip(self, tx), fields(db.operation = "insert", db.table = "events.public_event"))]
    async fn create(
        &self,
        tx: &DatabaseTransaction,
        cmd: CreatePublicEventRequest,
    ) -> Result<Uuid, Box<dyn std::error::Error>> {
        let id = Uuid::new_v4();

        // Insert into events.public_event (direct columns)
        let model = crate::entity::events_public_event::ActiveModel {
            id: Set(id),
            is_published: Set(cmd.is_published),
            capacity: Set(cmd.capacity),
            title: Set(cmd.title),
            birth_date: Set(cmd.birth_date),
            family_name: Set(cmd.family_name),
            given_name: Set(cmd.given_name),
            ..Default::default()
        };
        model.insert(tx).await?;

        Ok(id)
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select", db.table = "events.public_event"))]
    async fn find_by_id(
        &self,
        db: &DatabaseTransaction,
        id: Uuid,
        include_deleted: bool,
    ) -> Result<Option<PublicEventResponse>, Box<dyn std::error::Error>> {
        let mut query = crate::entity::events_public_event::Entity::find()
            .filter(crate::entity::events_public_event::Column::Id.eq(id));

        if !include_deleted {
            query = query.filter(crate::entity::events_public_event::Column::DeletedAt.is_null());
        }

        let row = query.one(db)
            .await?;

        let row = match row {
            Some(r) => r,
            None => return Ok(None),
        };

        Ok(Some(PublicEventResponse {
            id: row.id,
            is_published: row.is_published,
            capacity: row.capacity,
            title: row.title,
            birth_date: row.birth_date,
            family_name: row.family_name,
            given_name: row.given_name,
            created_at: row.created_at,
            updated_at: row.updated_at,
            ..Default::default()
        }))
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "update", db.table = "events.public_event"))]
    async fn update(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
        cmd: UpdatePublicEventRequest,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Update events.public_event — only set fields present in the update request
        let mut model = crate::entity::events_public_event::ActiveModel {
            id: Set(id),
            ..Default::default()
        };
        if let Some(v) = cmd.is_published { model.is_published = Set(Some(v)); }
        if let Some(v) = cmd.capacity { model.capacity = Set(Some(v)); }
        if let Some(v) = cmd.title { model.title = Set(v); }
        if let Some(v) = cmd.birth_date { model.birth_date = Set(Some(v)); }
        if let Some(v) = cmd.family_name { model.family_name = Set(v); }
        if let Some(v) = cmd.given_name { model.given_name = Set(v); }
        match model.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }

        Ok(())
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "delete", db.table = "events.public_event"))]
    async fn delete(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>> {
        let model = crate::entity::events_public_event::Entity::find()
            .filter(crate::entity::events_public_event::Column::Id.eq(id))
            .filter(crate::entity::events_public_event::Column::DeletedAt.is_null())
            .one(tx)
            .await?
            .ok_or_else(|| Box::<dyn std::error::Error>::from("Entity not found or already deleted"))?;
        let mut active: crate::entity::events_public_event::ActiveModel = model.into();
        active.deleted_at = sea_orm::ActiveValue::Set(Some(chrono::Utc::now().into()));
        match active.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }
        Ok(())
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select_list", db.table = "events.public_event"))]
    async fn list(
        &self,
        db: &DatabaseTransaction,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,
        include_deleted: bool,
    ) -> Result<(Vec<PublicEventResponse>, u64), Box<dyn std::error::Error>> {
        let mut query = crate::entity::events_public_event::Entity::find()
;
        if !include_deleted {
            query = query.filter(crate::entity::events_public_event::Column::DeletedAt.is_null());
        }
        let query = query.order_by_desc(crate::entity::events_public_event::Column::CreatedAt);
        let paginator = query.paginate(db, page_size);

        let total = paginator.num_items().await?;
        let rows = paginator.fetch_page(page).await?;

        let mut results = Vec::with_capacity(rows.len());
        for row in rows {
            results.push(PublicEventResponse {
                id: row.id,
                is_published: row.is_published,
                capacity: row.capacity,
                title: row.title,
                birth_date: row.birth_date,
                family_name: row.family_name,
                given_name: row.given_name,
                created_at: row.created_at,
                updated_at: row.updated_at,
                ..Default::default()
            });
        }

        Ok((results, total))
    }
}
