//! Generated repository implementation for ProcessHistoryItem.
//! DO NOT EDIT — generated by codegraph.

use async_trait::async_trait;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseTransaction,
    EntityTrait, PaginatorTrait, QueryFilter, QueryOrder, Set,
};
use uuid::Uuid;

use super::repository::ProcessHistoryItemRepository;
use super::dto_create::CreateProcessHistoryItemRequest;
use super::dto_update::UpdateProcessHistoryItemRequest;
use super::dto_response::ProcessHistoryItemResponse;

pub struct ProcessHistoryItemRepositoryImpl;

#[async_trait]
impl ProcessHistoryItemRepository for ProcessHistoryItemRepositoryImpl {
    #[tracing::instrument(skip(self, tx), fields(db.operation = "insert", db.table = "common.process_history_item"))]
    async fn create(
        &self,
        tx: &DatabaseTransaction,
        cmd: CreateProcessHistoryItemRequest,
    ) -> Result<Uuid, Box<dyn std::error::Error>> {
        let id = Uuid::new_v4();

        // Insert into common.process_history_item (direct columns)
        let model = crate::entity::common_process_history_item::ActiveModel {
            id: Set(id),
            action_date: Set(cmd.action_date),
            descriptions: Set(cmd.descriptions),
            ..Default::default()
        };
        model.insert(tx).await?;

        Ok(id)
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select", db.table = "common.process_history_item"))]
    async fn find_by_id(
        &self,
        db: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<Option<ProcessHistoryItemResponse>, Box<dyn std::error::Error>> {
        let row = crate::entity::common_process_history_item::Entity::find()
            .filter(crate::entity::common_process_history_item::Column::Id.eq(id))
            .filter(crate::entity::common_process_history_item::Column::DeletedAt.is_null())
            .one(db)
            .await?;

        let row = match row {
            Some(r) => r,
            None => return Ok(None),
        };

        Ok(Some(ProcessHistoryItemResponse {
            id: row.id,
            action_date: row.action_date,
            descriptions: row.descriptions,
            created_at: row.created_at,
            updated_at: row.updated_at,
        }))
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "update", db.table = "common.process_history_item"))]
    async fn update(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
        cmd: UpdateProcessHistoryItemRequest,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Update common.process_history_item — only set fields present in the update request
        let mut model = crate::entity::common_process_history_item::ActiveModel {
            id: Set(id),
            ..Default::default()
        };
        if let Some(v) = cmd.action_date { model.action_date = Set(Some(v)); }
        if let Some(v) = cmd.descriptions { model.descriptions = Set(Some(v)); }
        match model.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }

        Ok(())
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "delete", db.table = "common.process_history_item"))]
    async fn delete(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>> {
        let model = crate::entity::common_process_history_item::Entity::find()
            .filter(crate::entity::common_process_history_item::Column::Id.eq(id))
            .filter(crate::entity::common_process_history_item::Column::DeletedAt.is_null())
            .one(tx)
            .await?
            .ok_or_else(|| Box::<dyn std::error::Error>::from("Entity not found or already deleted"))?;
        let mut active: crate::entity::common_process_history_item::ActiveModel = model.into();
        active.deleted_at = sea_orm::ActiveValue::Set(Some(chrono::Utc::now().into()));
        match active.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }
        Ok(())
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select_list", db.table = "common.process_history_item"))]
    async fn list(
        &self,
        db: &DatabaseTransaction,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,
    ) -> Result<(Vec<ProcessHistoryItemResponse>, u64), Box<dyn std::error::Error>> {
        let query = crate::entity::common_process_history_item::Entity::find()
            .filter(crate::entity::common_process_history_item::Column::DeletedAt.is_null())
            .order_by_desc(crate::entity::common_process_history_item::Column::CreatedAt);
        let paginator = query.paginate(db, page_size);

        let total = paginator.num_items().await?;
        let rows = paginator.fetch_page(page).await?;

        let mut results = Vec::with_capacity(rows.len());
        for row in rows {
            results.push(ProcessHistoryItemResponse {
                id: row.id,
                action_date: row.action_date,
                descriptions: row.descriptions,
                created_at: row.created_at,
                updated_at: row.updated_at,
            });
        }

        Ok((results, total))
    }
}
