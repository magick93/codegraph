//! Generated repository implementation for GenderCodeList.
//! DO NOT EDIT — generated by codegraph.

use async_trait::async_trait;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseTransaction,
    EntityTrait, PaginatorTrait, QueryFilter, QueryOrder, Set,
};
use uuid::Uuid;

use super::repository::GenderCodeListRepository;
use super::dto_create::CreateGenderCodeListRequest;
use super::dto_update::UpdateGenderCodeListRequest;
use super::dto_response::GenderCodeListResponse;

pub struct GenderCodeListRepositoryImpl;

#[async_trait]
impl GenderCodeListRepository for GenderCodeListRepositoryImpl {
    #[tracing::instrument(skip(self, tx), fields(db.operation = "insert", db.table = "common.gender_code_list"))]
    async fn create(
        &self,
        tx: &DatabaseTransaction,
        cmd: CreateGenderCodeListRequest,
    ) -> Result<Uuid, Box<dyn std::error::Error>> {
        let id = Uuid::new_v4();

        // Insert into common.gender_code_list (direct columns)
        let model = crate::entity::common_gender_code_list::ActiveModel {
            id: Set(id),
            code: Set(cmd.code),
            display_name: Set(cmd.display_name),
            sort_order: Set(cmd.sort_order),
            ..Default::default()
        };
        model.insert(tx).await?;

        Ok(id)
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select", db.table = "common.gender_code_list"))]
    async fn find_by_id(
        &self,
        db: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<Option<GenderCodeListResponse>, Box<dyn std::error::Error>> {
        let row = crate::entity::common_gender_code_list::Entity::find()
            .filter(crate::entity::common_gender_code_list::Column::Id.eq(id))
            .filter(crate::entity::common_gender_code_list::Column::DeletedAt.is_null())
            .one(db)
            .await?;

        let row = match row {
            Some(r) => r,
            None => return Ok(None),
        };

        Ok(Some(GenderCodeListResponse {
            id: row.id,
            code: row.code,
            display_name: row.display_name,
            sort_order: row.sort_order,
            created_at: row.created_at,
            updated_at: row.updated_at,
        }))
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "update", db.table = "common.gender_code_list"))]
    async fn update(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
        cmd: UpdateGenderCodeListRequest,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Update common.gender_code_list — only set fields present in the update request
        let mut model = crate::entity::common_gender_code_list::ActiveModel {
            id: Set(id),
            ..Default::default()
        };
        if let Some(v) = cmd.code { model.code = Set(v); }
        if let Some(v) = cmd.display_name { model.display_name = Set(v); }
        if let Some(v) = cmd.sort_order { model.sort_order = Set(Some(v)); }
        match model.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }

        Ok(())
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "delete", db.table = "common.gender_code_list"))]
    async fn delete(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>> {
        let model = crate::entity::common_gender_code_list::Entity::find()
            .filter(crate::entity::common_gender_code_list::Column::Id.eq(id))
            .filter(crate::entity::common_gender_code_list::Column::DeletedAt.is_null())
            .one(tx)
            .await?
            .ok_or_else(|| Box::<dyn std::error::Error>::from("Entity not found or already deleted"))?;
        let mut active: crate::entity::common_gender_code_list::ActiveModel = model.into();
        active.deleted_at = sea_orm::ActiveValue::Set(Some(chrono::Utc::now().into()));
        match active.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }
        Ok(())
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select_list", db.table = "common.gender_code_list"))]
    async fn list(
        &self,
        db: &DatabaseTransaction,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,
    ) -> Result<(Vec<GenderCodeListResponse>, u64), Box<dyn std::error::Error>> {
        let query = crate::entity::common_gender_code_list::Entity::find()
            .filter(crate::entity::common_gender_code_list::Column::DeletedAt.is_null())
            .order_by_desc(crate::entity::common_gender_code_list::Column::CreatedAt);
        let paginator = query.paginate(db, page_size);

        let total = paginator.num_items().await?;
        let rows = paginator.fetch_page(page).await?;

        let mut results = Vec::with_capacity(rows.len());
        for row in rows {
            results.push(GenderCodeListResponse {
                id: row.id,
                code: row.code,
                display_name: row.display_name,
                sort_order: row.sort_order,
                created_at: row.created_at,
                updated_at: row.updated_at,
            });
        }

        Ok((results, total))
    }
}
