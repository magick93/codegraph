//! Repository trait for Amount.
//! Generated by codegraph. DO NOT EDIT.

use async_trait::async_trait;
use uuid::Uuid;

use super::dto_response::AmountResponse;

use super::dto_create::CreateAmountRequest;


use super::dto_update::UpdateAmountRequest;


/// Repository contract for Amount, generic over the client/transaction
/// type `C` so a single trait serves every persistence backend:
/// - SeaORM: `C = sea_orm::DatabaseTransaction`
/// - Cornucopia: `C: cornucopia_queries::client::GenericClient` (tokio-postgres
///   clients and transactions)
#[async_trait]
pub trait AmountRepository<C>: Send + Sync {

    async fn create(
        &self,
        tx: &C,
        cmd: CreateAmountRequest,

    ) -> Result<Uuid, Box<dyn std::error::Error>>;



    async fn find_by_id(
        &self,
        db: &C,
        id: Uuid,

        include_deleted: bool,

    ) -> Result<Option<AmountResponse>, Box<dyn std::error::Error>>;






    async fn update(
        &self,
        tx: &C,
        id: Uuid,
        cmd: UpdateAmountRequest,
    ) -> Result<(), Box<dyn std::error::Error>>;



    async fn delete(
        &self,
        tx: &C,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>>;



    async fn list(
        &self,
        db: &C,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,

        include_deleted: bool,

    ) -> Result<(Vec<AmountResponse>, u64), Box<dyn std::error::Error>>;




}
