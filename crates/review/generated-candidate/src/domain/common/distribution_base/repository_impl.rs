//! Generated repository implementation for DistributionBase.
//! DO NOT EDIT — generated by codegraph.

use async_trait::async_trait;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseTransaction,
    EntityTrait, PaginatorTrait, QueryFilter, QueryOrder, Set,
};
use uuid::Uuid;

use super::repository::DistributionBaseRepository;
use super::dto_create::CreateDistributionBaseRequest;
use super::dto_update::UpdateDistributionBaseRequest;
use super::dto_response::DistributionBaseResponse;

pub struct DistributionBaseRepositoryImpl;

#[async_trait]
impl DistributionBaseRepository for DistributionBaseRepositoryImpl {
    #[tracing::instrument(skip(self, tx), fields(db.operation = "insert", db.table = "common.distribution_base"))]
    async fn create(
        &self,
        tx: &DatabaseTransaction,
        cmd: CreateDistributionBaseRequest,
    ) -> Result<Uuid, Box<dyn std::error::Error>> {
        let id = Uuid::new_v4();

        // Insert into common.distribution_base (direct columns)
        let model = crate::entity::common_distribution_base::ActiveModel {
            id: Set(id),
            description: Set(cmd.description),
            end_date: Set(cmd.end_date),
            start_date: Set(cmd.start_date),
            ..Default::default()
        };
        model.insert(tx).await?;

        Ok(id)
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select", db.table = "common.distribution_base"))]
    async fn find_by_id(
        &self,
        db: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<Option<DistributionBaseResponse>, Box<dyn std::error::Error>> {
        let row = crate::entity::common_distribution_base::Entity::find()
            .filter(crate::entity::common_distribution_base::Column::Id.eq(id))
            .filter(crate::entity::common_distribution_base::Column::DeletedAt.is_null())
            .one(db)
            .await?;

        let row = match row {
            Some(r) => r,
            None => return Ok(None),
        };

        Ok(Some(DistributionBaseResponse {
            id: row.id,
            description: row.description,
            end_date: row.end_date,
            start_date: row.start_date,
            created_at: row.created_at,
            updated_at: row.updated_at,
        }))
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "update", db.table = "common.distribution_base"))]
    async fn update(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
        cmd: UpdateDistributionBaseRequest,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Update common.distribution_base — only set fields present in the update request
        let mut model = crate::entity::common_distribution_base::ActiveModel {
            id: Set(id),
            ..Default::default()
        };
        if let Some(v) = cmd.description { model.description = Set(Some(v)); }
        if let Some(v) = cmd.end_date { model.end_date = Set(Some(v)); }
        if let Some(v) = cmd.start_date { model.start_date = Set(v); }
        match model.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }

        Ok(())
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "delete", db.table = "common.distribution_base"))]
    async fn delete(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>> {
        let model = crate::entity::common_distribution_base::Entity::find()
            .filter(crate::entity::common_distribution_base::Column::Id.eq(id))
            .filter(crate::entity::common_distribution_base::Column::DeletedAt.is_null())
            .one(tx)
            .await?
            .ok_or_else(|| Box::<dyn std::error::Error>::from("Entity not found or already deleted"))?;
        let mut active: crate::entity::common_distribution_base::ActiveModel = model.into();
        active.deleted_at = sea_orm::ActiveValue::Set(Some(chrono::Utc::now().into()));
        match active.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }
        Ok(())
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select_list", db.table = "common.distribution_base"))]
    async fn list(
        &self,
        db: &DatabaseTransaction,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,
    ) -> Result<(Vec<DistributionBaseResponse>, u64), Box<dyn std::error::Error>> {
        let query = crate::entity::common_distribution_base::Entity::find()
            .filter(crate::entity::common_distribution_base::Column::DeletedAt.is_null())
            .order_by_desc(crate::entity::common_distribution_base::Column::CreatedAt);
        let paginator = query.paginate(db, page_size);

        let total = paginator.num_items().await?;
        let rows = paginator.fetch_page(page).await?;

        let mut results = Vec::with_capacity(rows.len());
        for row in rows {
            results.push(DistributionBaseResponse {
                id: row.id,
                description: row.description,
                end_date: row.end_date,
                start_date: row.start_date,
                created_at: row.created_at,
                updated_at: row.updated_at,
            });
        }

        Ok((results, total))
    }
}
