//! Generated repository implementation for Identifier.
//! DO NOT EDIT — generated by codegraph.

use async_trait::async_trait;
use sea_orm::{
    ActiveModelTrait, ColumnTrait, DatabaseTransaction,
    EntityTrait, PaginatorTrait, QueryFilter, QueryOrder, Set,
};
use uuid::Uuid;

use super::repository::IdentifierRepository;
use super::dto_create::CreateIdentifierRequest;
use super::dto_update::UpdateIdentifierRequest;
use super::dto_response::IdentifierResponse;

pub struct IdentifierRepositoryImpl;

#[async_trait]
impl IdentifierRepository for IdentifierRepositoryImpl {
    #[tracing::instrument(skip(self, tx), fields(db.operation = "insert", db.table = "common.identifier"))]
    async fn create(
        &self,
        tx: &DatabaseTransaction,
        cmd: CreateIdentifierRequest,
    ) -> Result<Uuid, Box<dyn std::error::Error>> {
        let id = Uuid::new_v4();

        // Insert into common.identifier (direct columns)
        let model = crate::entity::common_identifier::ActiveModel {
            id: Set(id),
            scheme_agency_id: Set(cmd.scheme_agency_id),
            scheme_id: Set(cmd.scheme_id),
            scheme_version_id: Set(cmd.scheme_version_id),
            value: Set(cmd.value),
            ..Default::default()
        };
        model.insert(tx).await?;

        Ok(id)
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select", db.table = "common.identifier"))]
    async fn find_by_id(
        &self,
        db: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<Option<IdentifierResponse>, Box<dyn std::error::Error>> {
        let row = crate::entity::common_identifier::Entity::find()
            .filter(crate::entity::common_identifier::Column::Id.eq(id))
            .filter(crate::entity::common_identifier::Column::DeletedAt.is_null())
            .one(db)
            .await?;

        let row = match row {
            Some(r) => r,
            None => return Ok(None),
        };

        Ok(Some(IdentifierResponse {
            id: row.id,
            scheme_agency_id: row.scheme_agency_id,
            scheme_id: row.scheme_id,
            scheme_version_id: row.scheme_version_id,
            value: row.value,
            created_at: row.created_at,
            updated_at: row.updated_at,
        }))
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "update", db.table = "common.identifier"))]
    async fn update(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
        cmd: UpdateIdentifierRequest,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Update common.identifier — only set fields present in the update request
        let mut model = crate::entity::common_identifier::ActiveModel {
            id: Set(id),
            ..Default::default()
        };
        if let Some(v) = cmd.scheme_agency_id { model.scheme_agency_id = Set(Some(v)); }
        if let Some(v) = cmd.scheme_id { model.scheme_id = Set(Some(v)); }
        if let Some(v) = cmd.scheme_version_id { model.scheme_version_id = Set(Some(v)); }
        if let Some(v) = cmd.value { model.value = Set(v); }
        match model.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }

        Ok(())
    }

    #[tracing::instrument(skip(self, tx), fields(db.operation = "delete", db.table = "common.identifier"))]
    async fn delete(
        &self,
        tx: &DatabaseTransaction,
        id: Uuid,
    ) -> Result<(), Box<dyn std::error::Error>> {
        let model = crate::entity::common_identifier::Entity::find()
            .filter(crate::entity::common_identifier::Column::Id.eq(id))
            .filter(crate::entity::common_identifier::Column::DeletedAt.is_null())
            .one(tx)
            .await?
            .ok_or_else(|| Box::<dyn std::error::Error>::from("Entity not found or already deleted"))?;
        let mut active: crate::entity::common_identifier::ActiveModel = model.into();
        active.deleted_at = sea_orm::ActiveValue::Set(Some(chrono::Utc::now().into()));
        match active.update(tx).await {
            Ok(_) => {}
            Err(sea_orm::DbErr::RecordNotUpdated) => { /* RLS hid the row — find_by_id will return 404 */ }
            Err(e) => return Err(e.into()),
        }
        Ok(())
    }

    #[tracing::instrument(skip(self, db), fields(db.operation = "select_list", db.table = "common.identifier"))]
    async fn list(
        &self,
        db: &DatabaseTransaction,
        page: u64,
        page_size: u64,
        filters: &std::collections::HashMap<String, String>,
    ) -> Result<(Vec<IdentifierResponse>, u64), Box<dyn std::error::Error>> {
        let mut condition = sea_orm::Condition::all();
        if let Some(val) = filters.get("scheme_agency_id") {
            condition = condition.add(crate::entity::common_identifier::Column::SchemeAgencyId.eq(val.clone()));
        }
        if let Some(val) = filters.get("scheme_id") {
            condition = condition.add(crate::entity::common_identifier::Column::SchemeId.eq(val.clone()));
        }
        if let Some(val) = filters.get("scheme_version_id") {
            condition = condition.add(crate::entity::common_identifier::Column::SchemeVersionId.eq(val.clone()));
        }
        let query = crate::entity::common_identifier::Entity::find()
            .filter(condition)
            .filter(crate::entity::common_identifier::Column::DeletedAt.is_null())
            .order_by_desc(crate::entity::common_identifier::Column::CreatedAt);
        let paginator = query.paginate(db, page_size);

        let total = paginator.num_items().await?;
        let rows = paginator.fetch_page(page).await?;

        let mut results = Vec::with_capacity(rows.len());
        for row in rows {
            results.push(IdentifierResponse {
                id: row.id,
                scheme_agency_id: row.scheme_agency_id,
                scheme_id: row.scheme_id,
                scheme_version_id: row.scheme_version_id,
                value: row.value,
                created_at: row.created_at,
                updated_at: row.updated_at,
            });
        }

        Ok((results, total))
    }
}
