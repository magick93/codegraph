// Generated by codegraph. DO NOT EDIT.
// CRUD E2E tests for Candidate.

import { test, expect } from '../../e2e/fixtures/personas';
import type { OrgContext } from '../../e2e/fixtures/personas';
import {
  createEntityAsAcme,
  createEntityViaApi,
  deleteEntityViaApi,
  bulkCreateEntitiesAsAcme,
  bulkCreateRaw,
  expectToast,
  expectTableContains,
  expectTableNotContains,
  waitForHydration,
} from '../../e2e/helpers';


const BASE_PATH = '/recruiting/candidate';


// Entity reference dependency IDs — populated in beforeAll when FK deps exist

const depIds: Record<string, string> = {};



function testData(): Record<string, unknown> {

  return {


    'birth_date': '2025-01-15',



    'family_name': 'Test Family Name',



    'given_name': 'Test Given Name',



    'application_process_history': 'Test Application Process History',



    'candidate_id': 'Test Candidate Id',



    'compensation_expectation': 42,



    'compensation_expectation_currency': 'USD',



    'distribution_guidelines': 'Test Distribution Guidelines',



    'external_identifier': { value: 'Test External Identifier' },



    'gender': 'Male',



    'person_name': 'Test Person Name',



    'position_schedule_type_codes': [{ code: 'FullTime' }],



    'position_titles': ['Test Position Titles'],



    'qualifications': ['Test Qualifications'],



    ...(depIds['referred_by_application_id_id'] ? { 'referred_by_application_id_id': depIds['referred_by_application_id_id'] } : {}),



    'status': 'active',



    'uri': 'Test Uri',


  };
}


function updatedData(): Record<string, unknown> {

  return {


    'birth_date': '2025-06-20',



    'family_name': 'Updated Family Name',



    'given_name': 'Updated Given Name',



    'application_process_history': 'Updated Application Process History',



    'candidate_id': 'Updated Candidate Id',



    'compensation_expectation': 99,



    'compensation_expectation_currency': 'AUD',



    'distribution_guidelines': 'Updated Distribution Guidelines',



    'external_identifier': { value: 'Updated External Identifier' },



    'gender': 'Other',



    'person_name': 'Updated Person Name',



    'position_schedule_type_codes': [{ code: 'SharedTime' }],



    'position_titles': ['Updated Position Titles'],



    'qualifications': ['Updated Qualifications'],



    ...(depIds['referred_by_application_id_id'] ? { 'referred_by_application_id_id': depIds['referred_by_application_id_id'] } : {}),



    'status': 'withdrawn',



    'uri': 'Updated Uri',


  };
}

test.describe.serial('Candidate CRUD', () => {
  let createdId: string | undefined;


  test.beforeAll(async ({ orgContext }) => {


    try {
      const dep_1 = await createEntityAsAcme(orgContext, '/recruiting/application', {  });
      depIds['referred_by_application_id_id'] = dep_1['id'] as string;
    } catch (_e) {
      // Dependency entity may already exist or have its own required fields
    }

  });

  test.afterAll(async ({ orgContext }) => {
    const baseUrl = process.env.PUBLIC_API_URL ?? 'http://localhost:3000';

    if (depIds['referred_by_application_id_id']) {
      try {
        await fetch(`${baseUrl}/api/recruiting/application/${depIds['referred_by_application_id_id']}`, {
          method: 'DELETE',
          headers: { 'Authorization': `Bearer ${orgContext.acme.apiKey}` },
        });
      } catch { /* best effort */ }
    }


  });


  test('list page loads with table', async ({ ownerPage: page }) => {
    await page.goto(BASE_PATH);
    // Either the data table or the empty-state placeholder should be visible
    const table = page.locator('[data-testid="candidate-table"]');
    const empty = page.locator('[data-testid="candidate-empty"]');
    await expect(table.or(empty)).toBeVisible();
  });



  test('create entity via form', async ({ ownerPage: page }) => {

    await page.goto(`${BASE_PATH}/new`);

    // Wait for SvelteKit to hydrate so the form's onsubmit handler is attached
    await waitForHydration(page, '[data-testid="candidate-submit-btn"]');
    const data = testData();



    if (await page.locator('#birth_date').isVisible()) {
      await page.locator('#birth_date').fill(String(data['birth_date']));
    }



    if (await page.locator('#family_name').isVisible()) {
      await page.locator('#family_name').fill(String(data['family_name']));
    }



    if (await page.locator('#given_name').isVisible()) {
      await page.locator('#given_name').fill(String(data['given_name']));
    }



    if (await page.locator('#application_process_history').isVisible()) {
      await page.locator('#application_process_history').fill(String(data['application_process_history']));
    }



    if (await page.locator('#candidate_id').isVisible()) {
      await page.locator('#candidate_id').fill(String(data['candidate_id']));
    }



    if (await page.locator('#compensation_expectation').isVisible()) {
      await page.locator('#compensation_expectation').fill(String(data['compensation_expectation']));
    }



    if (await page.locator('#compensation_expectation_currency').isVisible()) {
      await page.locator('#compensation_expectation_currency').selectOption(String(data['compensation_expectation_currency']));
    }



    if (await page.locator('#distribution_guidelines').isVisible()) {
      await page.locator('#distribution_guidelines').fill(String(data['distribution_guidelines']));
    }



    if (await page.locator('[data-testid="external_identifier-value"]').isVisible()) {
      await page.locator('[data-testid="external_identifier-value"]').fill(String((data['external_identifier'] as Record<string, unknown>)?.['value'] ?? ''));
    }



    if (await page.locator('#gender').isVisible()) {
      await page.locator('#gender').selectOption(String(data['gender']));
    }



    if (await page.locator('#person_name').isVisible()) {
      await page.locator('#person_name').fill(String(data['person_name']));
    }



    if (await page.locator('#position_schedule_type_codes').isVisible()) {
      await page.locator('#position_schedule_type_codes').selectOption('FullTime');
    }



    if (await page.locator('[data-testid="position_titles-row-0"]').isVisible()) {
      const vals = data['position_titles'] as string[];
      for (let i = 0; i < vals.length; i++) {
        if (i > 0) await page.locator('[data-testid="position_titles-add-btn"]').click();
        await page.locator(`[data-testid="position_titles-row-${i}"] input`).fill(vals[i]);
      }
    }



    if (await page.locator('#qualifications').isVisible()) {
      await page.locator('#qualifications').fill(String(data['qualifications']));
    }



    if (data['referred_by_application_id_id'] && await page.locator('#referred_by_application_id_id').isVisible()) {
      await page.locator('#referred_by_application_id_id').fill(String(data['referred_by_application_id_id']));
    }



    if (await page.locator('#status').isVisible()) {
      await page.locator('#status').selectOption(String(data['status']));
    }



    if (await page.locator('#uri').isVisible()) {
      await page.locator('#uri').fill(String(data['uri']));
    }



    await page.locator('[data-testid="candidate-submit-btn"]').click();
    await expectToast(page, 'success', 'success');

    // Should redirect to detail page

    await expect(page).toHaveURL(/\/recruiting\/candidate\/[0-9a-f-]+/);


    // Capture the created entity ID from the URL
    const url = page.url();
    createdId = url.split('/').pop()!;

    // Verify field values on detail page (only check fields present on the page)


    if (await page.locator('[data-testid="candidate-field-birth_date"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-birth_date"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-family_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-family_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-given_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-given_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-application_process_history"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-application_process_history"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-candidate_id"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-candidate_id"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-compensation_expectation"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-compensation_expectation"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-compensation_expectation_currency"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-compensation_expectation_currency"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-distribution_guidelines"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-distribution_guidelines"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-external_identifier"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-external_identifier"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-gender"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-gender"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-person_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-person_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-position_schedule_type_codes"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-position_schedule_type_codes"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-position_titles"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-position_titles"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-qualifications"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-qualifications"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-referred_by_application_id_id"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-referred_by_application_id_id"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-status"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-status"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-uri"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-uri"]')).toBeVisible();
    }


  });

  test('entity appears in list after create', async ({ ownerPage: page }) => {
    await page.goto(BASE_PATH);
    const table = page.locator('[data-testid="candidate-table"]');
    const empty = page.locator('[data-testid="candidate-empty"]');
    await expect(table.or(empty)).toBeVisible();
    // If we created an entity, the table should be visible (not empty state)
    if (createdId) {
      await expect(table).toBeVisible();
    }
  });




  test('bulk create via API returns 207 with success array', async ({ orgContext }) => {
    const items = [testData(), testData()];
    const result = await bulkCreateEntitiesAsAcme(orgContext, BASE_PATH, items);
    expect(result.success).toHaveLength(2);
    expect(result.failed).toHaveLength(0);
    expect(result.success[0]).toHaveProperty('id');
    expect(result.success[1]).toHaveProperty('id');
    // IDs should be distinct
    expect(result.success[0]['id']).not.toEqual(result.success[1]['id']);
  });

  test('bulk create items appear in list', async ({ ownerPage: page, orgContext }) => {
    const items = [testData(), testData()];
    await bulkCreateEntitiesAsAcme(orgContext, BASE_PATH, items);
    await page.goto(BASE_PATH);
    const table = page.locator('[data-testid="candidate-table"]');
    await expect(table).toBeVisible();
  });

  test('bulk create with empty array returns 400', async ({ orgContext }) => {
    const res = await bulkCreateRaw(BASE_PATH, [], {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${orgContext.acme.apiKey}`,
    });
    expect(res.status).toBe(400);
  });



  test('detail page shows all fields', async ({ ownerPage: page, orgContext }) => {
    // Create via API if we don't have one yet
    if (!createdId) {
      const entity = await createEntityAsAcme(orgContext, BASE_PATH, testData());
      createdId = entity['id'] as string;
    }


    await page.goto(`${BASE_PATH}/${createdId}`);

    await expect(page.getByRole('heading', { name: 'Candidate' })).toBeVisible();


    await expect(page.locator('[data-testid="candidate-field-birth_date"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-family_name"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-given_name"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-application_process_history"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-candidate_id"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-compensation_expectation"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-compensation_expectation_currency"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-distribution_guidelines"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-external_identifier"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-gender"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-person_name"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-position_schedule_type_codes"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-position_titles"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-qualifications"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-referred_by_application_id_id"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-status"]')).toBeVisible();

    await expect(page.locator('[data-testid="candidate-field-uri"]')).toBeVisible();


    // Timestamps should be present
    await expect(page.getByText('Created At')).toBeVisible();
    await expect(page.getByText('Updated')).toBeVisible();
  });




  test('edit entity via form', async ({ ownerPage: page, orgContext }) => {
    if (!createdId) {
      const entity = await createEntityAsAcme(orgContext, BASE_PATH, testData());
      createdId = entity['id'] as string;
    }


    await page.goto(`${BASE_PATH}/${createdId}/edit`);

    await waitForHydration(page, '[data-testid="candidate-submit-btn"]');
    const data = updatedData();



    if (await page.locator('#birth_date').isVisible()) {
      await page.locator('#birth_date').clear();
      await page.locator('#birth_date').fill(String(data['birth_date']));
    }



    if (await page.locator('#family_name').isVisible()) {
      await page.locator('#family_name').clear();
      await page.locator('#family_name').fill(String(data['family_name']));
    }



    if (await page.locator('#given_name').isVisible()) {
      await page.locator('#given_name').clear();
      await page.locator('#given_name').fill(String(data['given_name']));
    }



    if (await page.locator('#application_process_history').isVisible()) {
      await page.locator('#application_process_history').clear();
      await page.locator('#application_process_history').fill(String(data['application_process_history']));
    }



    if (await page.locator('#candidate_id').isVisible()) {
      await page.locator('#candidate_id').clear();
      await page.locator('#candidate_id').fill(String(data['candidate_id']));
    }



    if (await page.locator('#compensation_expectation').isVisible()) {
      await page.locator('#compensation_expectation').clear();
      await page.locator('#compensation_expectation').fill(String(data['compensation_expectation']));
    }



    if (await page.locator('#compensation_expectation_currency').isVisible()) {
      await page.locator('#compensation_expectation_currency').selectOption('AUD');
    }



    if (await page.locator('#distribution_guidelines').isVisible()) {
      await page.locator('#distribution_guidelines').clear();
      await page.locator('#distribution_guidelines').fill(String(data['distribution_guidelines']));
    }



    if (await page.locator('[data-testid="external_identifier-value"]').isVisible()) {
      await page.locator('[data-testid="external_identifier-value"]').clear();
      await page.locator('[data-testid="external_identifier-value"]').fill(String((data['external_identifier'] as Record<string, unknown>)?.['value'] ?? ''));
    }



    if (await page.locator('#gender').isVisible()) {
      await page.locator('#gender').selectOption('Other');
    }



    if (await page.locator('#person_name').isVisible()) {
      await page.locator('#person_name').clear();
      await page.locator('#person_name').fill(String(data['person_name']));
    }



    if (await page.locator('#position_schedule_type_codes').isVisible()) {
      await page.locator('#position_schedule_type_codes').selectOption('SharedTime');
    }



    if (await page.locator('[data-testid="position_titles-row-0"]').isVisible()) {
      const vals = data['position_titles'] as string[];
      // Clear existing rows first
      const existingRows = await page.locator('[data-testid^="position_titles-row-"]').count();
      for (let i = existingRows - 1; i > 0; i--) {
        await page.locator(`[data-testid="position_titles-remove-${i}"]`).click();
      }
      await page.locator('[data-testid="position_titles-row-0"] input').clear();
      await page.locator('[data-testid="position_titles-row-0"] input').fill(vals[0]);
      for (let i = 1; i < vals.length; i++) {
        await page.locator('[data-testid="position_titles-add-btn"]').click();
        await page.locator(`[data-testid="position_titles-row-${i}"] input`).fill(vals[i]);
      }
    }



    if (await page.locator('#qualifications').isVisible()) {
      await page.locator('#qualifications').clear();
      await page.locator('#qualifications').fill(String(data['qualifications']));
    }



    if (data['referred_by_application_id_id'] && await page.locator('#referred_by_application_id_id').isVisible()) {
      await page.locator('#referred_by_application_id_id').clear();
      await page.locator('#referred_by_application_id_id').fill(String(data['referred_by_application_id_id']));
    }



    if (await page.locator('#status').isVisible()) {
      await page.locator('#status').selectOption('withdrawn');
    }



    if (await page.locator('#uri').isVisible()) {
      await page.locator('#uri').clear();
      await page.locator('#uri').fill(String(data['uri']));
    }



    await page.locator('[data-testid="candidate-submit-btn"]').click();
    await expectToast(page, 'success', 'success');

    // Should redirect to detail page

    await expect(page).toHaveURL(/\/recruiting\/candidate\/[0-9a-f-]+$/);


    // Verify updated values (only check fields present on the page)


    if (await page.locator('[data-testid="candidate-field-birth_date"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-birth_date"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-family_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-family_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-given_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-given_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-application_process_history"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-application_process_history"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-candidate_id"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-candidate_id"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-compensation_expectation"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-compensation_expectation"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-compensation_expectation_currency"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-compensation_expectation_currency"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-distribution_guidelines"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-distribution_guidelines"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-external_identifier"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-external_identifier"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-gender"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-gender"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-person_name"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-person_name"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-position_schedule_type_codes"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-position_schedule_type_codes"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-position_titles"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-position_titles"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-qualifications"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-qualifications"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-referred_by_application_id_id"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-referred_by_application_id_id"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-status"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-status"]')).toBeVisible();
    }



    if (await page.locator('[data-testid="candidate-field-uri"]').count() > 0) {
      await expect(page.locator('[data-testid="candidate-field-uri"]')).toBeVisible();
    }


  });




  test('detail page shows child sections', async ({ ownerPage: page, orgContext }) => {
    if (!createdId) {
      const entity = await createEntityAsAcme(orgContext, BASE_PATH, testData());
      createdId = entity['id'] as string;
    }


    await page.goto(`${BASE_PATH}/${createdId}`);


    await expect(page.locator('[data-testid="child-section-distribution_guidelines"]')).toBeVisible();

    await expect(page.locator('[data-testid="child-section-qualification"]')).toBeVisible();

  });





  test('404 for non-existent entity', async ({ ownerPage: page }) => {

    await page.goto(`${BASE_PATH}/00000000-0000-0000-0000-000000000000`);

    await expect(page.getByText(/not found|doesn.t exist/i)).toBeVisible();
  });

});
