<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for Application. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateApplicationRequest, UpdateApplicationRequest } from '$lib/api/types';

	type FormData = CreateApplicationRequest | UpdateApplicationRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let application_id = $state(String(initialData['application_id'] ?? ''));



	let applied_date = $state(String(initialData['applied_date'] ?? ''));



	let candidate_id_id = $state(String(initialData['candidate_id_id'] ?? ''));



	let status = $state(String(initialData['status'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			data['application_id'] = application_id;



			if (applied_date) data['applied_date'] = applied_date;



			if (candidate_id_id) data['candidate_id_id'] = candidate_id_id;



			if (status) data['status'] = status;


		} else {
			// Edit mode: only send changed fields


			if (application_id !== String(initialData['application_id'] ?? '')) data['application_id'] = application_id;



			if (applied_date !== String(initialData['applied_date'] ?? '')) data['applied_date'] = applied_date;



			if (candidate_id_id !== String(initialData['candidate_id_id'] ?? '')) data['candidate_id_id'] = candidate_id_id;



			if (status !== String(initialData['status'] ?? '')) data['status'] = status;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.recruiting_application_new() : m.common_edit() + ' ' + m.recruiting_application_title()} data-testid="application-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="application_id">{m.recruiting_application_field_application_id()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="application_id"
				type="text"
				bind:value={application_id}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="applied_date">{m.recruiting_application_field_applied_date()}</Field.Label>
			<Input
				id="applied_date"
				type="date"
				bind:value={applied_date}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="candidate_id_id">{m.recruiting_application_field_candidate_id_id()}</Field.Label>
			<Input
				id="candidate_id_id"
				type="text"
				bind:value={candidate_id_id}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="status">{m.recruiting_application_field_status()}</Field.Label>
			<NativeSelect.Root id="status" bind:value={status} disabled={mode === 'edit' && false}>
				<NativeSelect.Option value="" disabled={false}>{m.common_select_placeholder()}</NativeSelect.Option>

				<NativeSelect.Option value="Applied">Applied</NativeSelect.Option>

				<NativeSelect.Option value="Screening">Screening</NativeSelect.Option>

				<NativeSelect.Option value="Interviewing">Interviewing</NativeSelect.Option>

				<NativeSelect.Option value="Offered">Offered</NativeSelect.Option>

				<NativeSelect.Option value="Hired">Hired</NativeSelect.Option>

				<NativeSelect.Option value="Rejected">Rejected</NativeSelect.Option>

			</NativeSelect.Root>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="application-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="application-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
