<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for Name. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateNameRequest, UpdateNameRequest } from '$lib/api/types';

	type FormData = CreateNameRequest | UpdateNameRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let family_name = $state(String(initialData['family_name'] ?? ''));



	let formatted_name = $state(String(initialData['formatted_name'] ?? ''));



	let given_name = $state(String(initialData['given_name'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			if (family_name) data['family_name'] = family_name;



			if (formatted_name) data['formatted_name'] = formatted_name;



			if (given_name) data['given_name'] = given_name;


		} else {
			// Edit mode: only send changed fields


			if (family_name !== String(initialData['family_name'] ?? '')) data['family_name'] = family_name;



			if (formatted_name !== String(initialData['formatted_name'] ?? '')) data['formatted_name'] = formatted_name;



			if (given_name !== String(initialData['given_name'] ?? '')) data['given_name'] = given_name;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.common_name_new() : m.common_edit() + ' ' + m.common_name_title()} data-testid="name-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="family_name">{m.common_name_field_family_name()}</Field.Label>
			<Input
				id="family_name"
				type="text"
				bind:value={family_name}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="formatted_name">{m.common_name_field_formatted_name()}</Field.Label>
			<Input
				id="formatted_name"
				type="text"
				bind:value={formatted_name}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="given_name">{m.common_name_field_given_name()}</Field.Label>
			<Input
				id="given_name"
				type="text"
				bind:value={given_name}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="name-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="name-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
