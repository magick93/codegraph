<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for DistributionBase. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateDistributionBaseRequest, UpdateDistributionBaseRequest } from '$lib/api/types';

	type FormData = CreateDistributionBaseRequest | UpdateDistributionBaseRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let description = $state(String(initialData['description'] ?? ''));



	let end_date = $state(String(initialData['end_date'] ?? ''));



	let start_date = $state(String(initialData['start_date'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			if (description) data['description'] = description;



			if (end_date) data['end_date'] = end_date;



			data['start_date'] = start_date;


		} else {
			// Edit mode: only send changed fields


			if (description !== String(initialData['description'] ?? '')) data['description'] = description;



			if (end_date !== String(initialData['end_date'] ?? '')) data['end_date'] = end_date;



			if (start_date !== String(initialData['start_date'] ?? '')) data['start_date'] = start_date;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.common_distribution_base_new() : m.common_edit() + ' ' + m.common_distribution_base_title()} data-testid="distribution_base-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="description">{m.common_distribution_base_field_description()}</Field.Label>
			<Input
				id="description"
				type="text"
				bind:value={description}
				
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>Distribution description</Field.Description>

		</Field.Field>



		<Field.Field>
			<Field.Label for="end_date">{m.common_distribution_base_field_end_date()}</Field.Label>
			<Input
				id="end_date"
				type="date"
				bind:value={end_date}
				
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>Distribution end date</Field.Description>

		</Field.Field>



		<Field.Field>
			<Field.Label for="start_date">{m.common_distribution_base_field_start_date()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="start_date"
				type="date"
				bind:value={start_date}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>Distribution start date</Field.Description>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="distribution_base-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="distribution_base-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
