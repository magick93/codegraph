<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for PublicEvent. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreatePublicEventRequest, UpdatePublicEventRequest } from '$lib/api/types';

	type FormData = CreatePublicEventRequest | UpdatePublicEventRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let is_published = $state(Boolean(initialData['is_published'] ?? false));



	let capacity = $state<number | undefined>(initialData['capacity'] != null ? Number(initialData['capacity']) : undefined);



	let title = $state(String(initialData['title'] ?? ''));



	let birth_date = $state(String(initialData['birth_date'] ?? ''));



	let family_name = $state(String(initialData['family_name'] ?? ''));



	let given_name = $state(String(initialData['given_name'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			data['is_published'] = is_published;



			if (capacity !== undefined && capacity !== null) data['capacity'] = capacity;



			data['title'] = title;



			if (birth_date) data['birth_date'] = birth_date;



			data['family_name'] = family_name;



			data['given_name'] = given_name;


		} else {
			// Edit mode: only send changed fields


			if (is_published !== Boolean(initialData['is_published'])) data['is_published'] = is_published;



			if (capacity !== Number(initialData['capacity'])) data['capacity'] = capacity;



			if (title !== String(initialData['title'] ?? '')) data['title'] = title;



			if (birth_date !== String(initialData['birth_date'] ?? '')) data['birth_date'] = birth_date;



			if (family_name !== String(initialData['family_name'] ?? '')) data['family_name'] = family_name;



			if (given_name !== String(initialData['given_name'] ?? '')) data['given_name'] = given_name;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.events_public_event_new() : m.common_edit() + ' ' + m.events_public_event_title()} data-testid="public_event-form">
	<Field.Group>


		<Field.Field orientation="horizontal">
			<Field.Label for="is_published">{m.events_public_event_field_is_published()}</Field.Label>
			<Switch
				id="is_published"
				checked={is_published}
				onCheckedChange={(v) => is_published = v}
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="capacity">{m.events_public_event_field_capacity()}</Field.Label>
			<Input
				id="capacity"
				type="number"
				bind:value={capacity}
				
				step="any"
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="title">{m.events_public_event_field_title()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="title"
				type="text"
				bind:value={title}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>Event title</Field.Description>

		</Field.Field>



		<Field.Field>
			<Field.Label for="birth_date">{m.events_public_event_field_birth_date()}</Field.Label>
			<Input
				id="birth_date"
				type="date"
				bind:value={birth_date}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="family_name">{m.events_public_event_field_family_name()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="family_name"
				type="text"
				bind:value={family_name}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>Last name</Field.Description>

		</Field.Field>



		<Field.Field>
			<Field.Label for="given_name">{m.events_public_event_field_given_name()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="given_name"
				type="text"
				bind:value={given_name}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

			<Field.Description>First name</Field.Description>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="public_event-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="public_event-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
