<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for PayRun. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreatePayRunRequest, UpdatePayRunRequest } from '$lib/api/types';

	type FormData = CreatePayRunRequest | UpdatePayRunRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let pay_run_id = $state(String(initialData['pay_run_id'] ?? ''));



	let run_date = $state(String(initialData['run_date'] ?? ''));



	let total_amount = $state<number | undefined>(initialData['total_amount'] != null ? Number(initialData['total_amount']) : undefined);



	let total_amount_currency = $state(String(initialData['total_amount_currency'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			data['pay_run_id'] = pay_run_id;



			if (run_date) data['run_date'] = run_date;



			if (total_amount !== undefined && total_amount !== null) data['total_amount'] = total_amount;



			if (total_amount_currency) data['total_amount_currency'] = total_amount_currency;


		} else {
			// Edit mode: only send changed fields


			if (pay_run_id !== String(initialData['pay_run_id'] ?? '')) data['pay_run_id'] = pay_run_id;



			if (run_date !== String(initialData['run_date'] ?? '')) data['run_date'] = run_date;



			if (total_amount !== Number(initialData['total_amount'])) data['total_amount'] = total_amount;



			if (total_amount_currency !== String(initialData['total_amount_currency'] ?? '')) data['total_amount_currency'] = total_amount_currency;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.compensation_pay_run_new() : m.common_edit() + ' ' + m.compensation_pay_run_title()} data-testid="pay_run-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="pay_run_id">{m.compensation_pay_run_field_pay_run_id()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="pay_run_id"
				type="text"
				bind:value={pay_run_id}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="run_date">{m.compensation_pay_run_field_run_date()}</Field.Label>
			<Input
				id="run_date"
				type="date"
				bind:value={run_date}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="total_amount">{m.compensation_pay_run_field_total_amount()}</Field.Label>
			<Input
				id="total_amount"
				type="number"
				bind:value={total_amount}
				
				step="any"
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="total_amount_currency">{m.compensation_pay_run_field_total_amount_currency()}</Field.Label>
			<NativeSelect.Root id="total_amount_currency" bind:value={total_amount_currency} disabled={mode === 'edit' && false}>
				<NativeSelect.Option value="" disabled={false}>{m.common_select_placeholder()}</NativeSelect.Option>

				<NativeSelect.Option value="USD">USD</NativeSelect.Option>

				<NativeSelect.Option value="EUR">EUR</NativeSelect.Option>

				<NativeSelect.Option value="GBP">GBP</NativeSelect.Option>

				<NativeSelect.Option value="JPY">JPY</NativeSelect.Option>

				<NativeSelect.Option value="AUD">AUD</NativeSelect.Option>

			</NativeSelect.Root>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="pay_run-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="pay_run-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
