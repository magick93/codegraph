<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for Identifier. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateIdentifierRequest, UpdateIdentifierRequest } from '$lib/api/types';

	type FormData = CreateIdentifierRequest | UpdateIdentifierRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let scheme_agency_id = $state(String(initialData['scheme_agency_id'] ?? ''));



	let scheme_id = $state(String(initialData['scheme_id'] ?? ''));



	let scheme_version_id = $state(String(initialData['scheme_version_id'] ?? ''));



	let value = $state(String(initialData['value'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			if (scheme_agency_id) data['scheme_agency_id'] = scheme_agency_id;



			if (scheme_id) data['scheme_id'] = scheme_id;



			if (scheme_version_id) data['scheme_version_id'] = scheme_version_id;



			data['value'] = value;


		} else {
			// Edit mode: only send changed fields


			if (scheme_agency_id !== String(initialData['scheme_agency_id'] ?? '')) data['scheme_agency_id'] = scheme_agency_id;



			if (scheme_id !== String(initialData['scheme_id'] ?? '')) data['scheme_id'] = scheme_id;



			if (scheme_version_id !== String(initialData['scheme_version_id'] ?? '')) data['scheme_version_id'] = scheme_version_id;



			if (value !== String(initialData['value'] ?? '')) data['value'] = value;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.common_identifier_new() : m.common_edit() + ' ' + m.common_identifier_title()} data-testid="identifier-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="scheme_agency_id">{m.common_identifier_field_scheme_agency_id()}</Field.Label>
			<Input
				id="scheme_agency_id"
				type="text"
				bind:value={scheme_agency_id}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="scheme_id">{m.common_identifier_field_scheme_id()}</Field.Label>
			<Input
				id="scheme_id"
				type="text"
				bind:value={scheme_id}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="scheme_version_id">{m.common_identifier_field_scheme_version_id()}</Field.Label>
			<Input
				id="scheme_version_id"
				type="text"
				bind:value={scheme_version_id}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="value">{m.common_identifier_field_value()} <span class="text-destructive">*</span></Field.Label>
			<Input
				id="value"
				type="text"
				bind:value={value}
				required aria-required="true"
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="identifier-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="identifier-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
