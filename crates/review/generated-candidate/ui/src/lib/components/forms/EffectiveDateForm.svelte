<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Form component for EffectiveDate. -->
<script lang="ts">
	import * as Field from '$lib/components/ui/field/index.js';
	import { Input } from '$lib/components/ui/input/index.js';
	import * as NativeSelect from '$lib/components/ui/native-select/index.js';
	import { Switch } from '$lib/components/ui/switch/index.js';
	import { Button } from '$lib/components/ui/button/index.js';
	import { Spinner } from '$lib/components/ui/spinner/index.js';
	import { DateRangeField } from '$lib/components/ui/date-range-field/index.js';
	import { ArrayField } from '$lib/components/ui/array-field/index.js';
	import { StructuredWrapperField } from '$lib/components/ui/structured-wrapper-field/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateEffectiveDateRequest, UpdateEffectiveDateRequest } from '$lib/api/types';

	type FormData = CreateEffectiveDateRequest | UpdateEffectiveDateRequest;

	interface Props {
		mode: 'create' | 'edit';
		initialData?: Record<string, unknown>;
		onSubmit: (data: FormData) => void;
		submitting?: boolean;
	}

	let { mode, initialData = {}, onSubmit, submitting = false }: Props = $props();

	// Form state


	let valid_from = $state(String(initialData['valid_from'] ?? ''));



	let valid_to = $state(String(initialData['valid_to'] ?? ''));



	function handleSubmit(e: Event) {
		e.preventDefault();
		const data: Record<string, unknown> = {};

		if (mode === 'create') {


			if (valid_from) data['valid_from'] = valid_from;



			if (valid_to) data['valid_to'] = valid_to;


		} else {
			// Edit mode: only send changed fields


			if (valid_from !== String(initialData['valid_from'] ?? '')) data['valid_from'] = valid_from;



			if (valid_to !== String(initialData['valid_to'] ?? '')) data['valid_to'] = valid_to;


		}

		onSubmit(data as FormData);
	}
</script>

<form onsubmit={handleSubmit} class="space-y-6" aria-label={mode === 'create' ? m.common_effective_date_new() : m.common_edit() + ' ' + m.common_effective_date_title()} data-testid="effective_date-form">
	<Field.Group>


		<Field.Field>
			<Field.Label for="valid_from">{m.common_effective_date_field_valid_from()}</Field.Label>
			<Input
				id="valid_from"
				type="date"
				bind:value={valid_from}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>



		<Field.Field>
			<Field.Label for="valid_to">{m.common_effective_date_field_valid_to()}</Field.Label>
			<Input
				id="valid_to"
				type="date"
				bind:value={valid_to}
				
				
				disabled={mode === 'edit' && false}
			/>

		</Field.Field>


	</Field.Group>



	<div class="flex flex-col-reverse gap-3 pt-4 sm:flex-row sm:justify-end">

		<Button variant="outline" type="button" onclick={() => history.back()} data-testid="effective_date-cancel-btn">
			{m.common_cancel()}
		</Button>
		<Button type="submit" disabled={submitting} data-testid="effective_date-submit-btn">
			{#if submitting}
				<Spinner class="mr-2 h-4 w-4" />
				{m.common_saving()}
			{:else}
				{mode === 'create' ? m.common_create() : m.common_save()}
			{/if}
		</Button>

	</div>
</form>
