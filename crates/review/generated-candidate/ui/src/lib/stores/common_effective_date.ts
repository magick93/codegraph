// Generated by codegraph. DO NOT EDIT.
// Svelte store for EffectiveDate CRUD operations.

import { writable } from 'svelte/store';
import { apiGet, apiPost, apiPut, apiDelete } from '$lib/api/client';
import type {
	EffectiveDateResponse,

	CreateEffectiveDateRequest,


	UpdateEffectiveDateRequest,


} from '$lib/api/types';


const API_PATH = '/common/effective-date';


export interface EffectiveDateListState {
	items: EffectiveDateResponse[];
	loading: boolean;
	error: string | null;
}

export const effective_dateList = writable<EffectiveDateListState>({
	items: [],
	loading: false,
	error: null
});



export async function listEffectiveDate(page = 0, pageSize = 20): Promise<EffectiveDateResponse[]> {

	effective_dateList.update(s => ({ ...s, loading: true, error: null }));
	try {

		const items = await apiGet<EffectiveDateResponse[]>(API_PATH, { page, page_size: pageSize });

		effective_dateList.set({ items, loading: false, error: null });
		return items;
	} catch (e) {
		const msg = (e as Error).message;
		effective_dateList.update(s => ({ ...s, loading: false, error: msg }));
		throw e;
	}
}




export async function getEffectiveDate(id: string): Promise<EffectiveDateResponse> {
	return apiGet<EffectiveDateResponse>(`${API_PATH}/${id}`);

}




export async function createEffectiveDate(data: CreateEffectiveDateRequest): Promise<EffectiveDateResponse> {
	const result = await apiPost<EffectiveDateResponse>(API_PATH, data);

	effective_dateList.update(s => ({ ...s, items: [...s.items, result] }));
	return result;
}




export async function updateEffectiveDate(id: string, data: UpdateEffectiveDateRequest): Promise<EffectiveDateResponse> {
	const result = await apiPut<EffectiveDateResponse>(`${API_PATH}/${id}`, data);

	effective_dateList.update(s => ({
		...s,
		items: s.items.map(item => item.id === id ? result : item)
	}));
	return result;
}




export async function deleteEffectiveDate(id: string): Promise<void> {
	await apiDelete(`${API_PATH}/${id}`);

	effective_dateList.update(s => ({
		...s,
		items: s.items.filter(item => item.id !== id)
	}));
}


