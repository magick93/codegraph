// Generated by codegraph. DO NOT EDIT.
// Svelte store for Identifier CRUD operations.

import { writable } from 'svelte/store';
import { apiGet, apiPost, apiPut, apiDelete } from '$lib/api/client';
import type {
	IdentifierResponse,

	CreateIdentifierRequest,


	UpdateIdentifierRequest,


} from '$lib/api/types';


const API_PATH = '/common/identifier';


export interface IdentifierListState {
	items: IdentifierResponse[];
	loading: boolean;
	error: string | null;
}

export const identifierList = writable<IdentifierListState>({
	items: [],
	loading: false,
	error: null
});



export async function listIdentifier(page = 0, pageSize = 20): Promise<IdentifierResponse[]> {

	identifierList.update(s => ({ ...s, loading: true, error: null }));
	try {

		const items = await apiGet<IdentifierResponse[]>(API_PATH, { page, page_size: pageSize });

		identifierList.set({ items, loading: false, error: null });
		return items;
	} catch (e) {
		const msg = (e as Error).message;
		identifierList.update(s => ({ ...s, loading: false, error: msg }));
		throw e;
	}
}




export async function getIdentifier(id: string): Promise<IdentifierResponse> {
	return apiGet<IdentifierResponse>(`${API_PATH}/${id}`);

}




export async function createIdentifier(data: CreateIdentifierRequest): Promise<IdentifierResponse> {
	const result = await apiPost<IdentifierResponse>(API_PATH, data);

	identifierList.update(s => ({ ...s, items: [...s.items, result] }));
	return result;
}




export async function updateIdentifier(id: string, data: UpdateIdentifierRequest): Promise<IdentifierResponse> {
	const result = await apiPut<IdentifierResponse>(`${API_PATH}/${id}`, data);

	identifierList.update(s => ({
		...s,
		items: s.items.map(item => item.id === id ? result : item)
	}));
	return result;
}




export async function deleteIdentifier(id: string): Promise<void> {
	await apiDelete(`${API_PATH}/${id}`);

	identifierList.update(s => ({
		...s,
		items: s.items.filter(item => item.id !== id)
	}));
}


