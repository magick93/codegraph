// Generated by codegraph. DO NOT EDIT.
// Svelte store for FormattedDateTime CRUD operations.

import { writable } from 'svelte/store';
import { apiGet, apiPost, apiPut, apiDelete } from '$lib/api/client';
import type {
	FormattedDateTimeResponse,

	CreateFormattedDateTimeRequest,


	UpdateFormattedDateTimeRequest,


} from '$lib/api/types';


const API_PATH = '/common/formatted-date-time';


export interface FormattedDateTimeListState {
	items: FormattedDateTimeResponse[];
	loading: boolean;
	error: string | null;
}

export const formatted_date_timeList = writable<FormattedDateTimeListState>({
	items: [],
	loading: false,
	error: null
});



export async function listFormattedDateTime(page = 0, pageSize = 20): Promise<FormattedDateTimeResponse[]> {

	formatted_date_timeList.update(s => ({ ...s, loading: true, error: null }));
	try {

		const items = await apiGet<FormattedDateTimeResponse[]>(API_PATH, { page, page_size: pageSize });

		formatted_date_timeList.set({ items, loading: false, error: null });
		return items;
	} catch (e) {
		const msg = (e as Error).message;
		formatted_date_timeList.update(s => ({ ...s, loading: false, error: msg }));
		throw e;
	}
}




export async function getFormattedDateTime(id: string): Promise<FormattedDateTimeResponse> {
	return apiGet<FormattedDateTimeResponse>(`${API_PATH}/${id}`);

}




export async function createFormattedDateTime(data: CreateFormattedDateTimeRequest): Promise<FormattedDateTimeResponse> {
	const result = await apiPost<FormattedDateTimeResponse>(API_PATH, data);

	formatted_date_timeList.update(s => ({ ...s, items: [...s.items, result] }));
	return result;
}




export async function updateFormattedDateTime(id: string, data: UpdateFormattedDateTimeRequest): Promise<FormattedDateTimeResponse> {
	const result = await apiPut<FormattedDateTimeResponse>(`${API_PATH}/${id}`, data);

	formatted_date_timeList.update(s => ({
		...s,
		items: s.items.map(item => item.id === id ? result : item)
	}));
	return result;
}




export async function deleteFormattedDateTime(id: string): Promise<void> {
	await apiDelete(`${API_PATH}/${id}`);

	formatted_date_timeList.update(s => ({
		...s,
		items: s.items.filter(item => item.id !== id)
	}));
}


