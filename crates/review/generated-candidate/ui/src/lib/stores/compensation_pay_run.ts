// Generated by hr-graph. DO NOT EDIT.
// Svelte store for PayRun CRUD operations.

import { writable } from 'svelte/store';
import { apiGet, apiPost, apiPut, apiDelete } from '$lib/api/client';
import type {
	PayRunResponse,

	CreatePayRunRequest,


	UpdatePayRunRequest,


} from '$lib/api/types';


const API_PATH = '/compensation/pay-run';


export interface PayRunListState {
	items: PayRunResponse[];
	loading: boolean;
	error: string | null;
}

export const pay_runList = writable<PayRunListState>({
	items: [],
	loading: false,
	error: null
});



export async function listPayRun(page = 0, pageSize = 20): Promise<PayRunResponse[]> {

	pay_runList.update(s => ({ ...s, loading: true, error: null }));
	try {

		const items = await apiGet<PayRunResponse[]>(API_PATH, { page, page_size: pageSize });

		pay_runList.set({ items, loading: false, error: null });
		return items;
	} catch (e) {
		const msg = (e as Error).message;
		pay_runList.update(s => ({ ...s, loading: false, error: msg }));
		throw e;
	}
}




export async function getPayRun(id: string): Promise<PayRunResponse> {
	return apiGet<PayRunResponse>(`${API_PATH}/${id}`);

}




export async function createPayRun(data: CreatePayRunRequest): Promise<PayRunResponse> {
	const result = await apiPost<PayRunResponse>(API_PATH, data);

	pay_runList.update(s => ({ ...s, items: [...s.items, result] }));
	return result;
}




export async function updatePayRun(id: string, data: UpdatePayRunRequest): Promise<PayRunResponse> {
	const result = await apiPut<PayRunResponse>(`${API_PATH}/${id}`, data);

	pay_runList.update(s => ({
		...s,
		items: s.items.map(item => item.id === id ? result : item)
	}));
	return result;
}




export async function deletePayRun(id: string): Promise<void> {
	await apiDelete(`${API_PATH}/${id}`);

	pay_runList.update(s => ({
		...s,
		items: s.items.filter(item => item.id !== id)
	}));
}


