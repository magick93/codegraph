<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Create page for DistributionBase. -->
<script lang="ts">
	import { goto } from '$app/navigation';

	import DistributionBaseForm from '$lib/components/forms/DistributionBaseForm.svelte';
	import { createDistributionBase } from '$lib/stores/common_distribution_base';
	import { toast } from 'svelte-sonner';
	import * as Card from '$lib/components/ui/card/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateDistributionBaseRequest } from '$lib/api/types';


	const basePath = '/common/distribution-base';


	let submitting = $state(false);
	let error = $state('');

	async function handleSubmit(formData: CreateDistributionBaseRequest) {
		submitting = true;
		error = '';
		try {

			const result = await createDistributionBase(formData);

			toast.success(m.common_entity_created({ entity: 'DistributionBase' }));
			goto(`${basePath}/${result.id}`);
		} catch (e) {
			error = (e as Error).message;
			toast.error(m.common_create_failed({ entity: 'DistributionBase' }));
		} finally {
			submitting = false;
		}
	}
</script>

<div class="space-y-6">
	<div>
		<a href={basePath} class="text-sm text-muted-foreground hover:text-foreground hover:underline">
			&larr; {m.common_back_to_list()}
		</a>
		<h2 class="mt-1 text-xl font-semibold">{m.common_distribution_base_new()}</h2>
	</div>


	{#if error}
		<div class="rounded-md bg-destructive/10 border border-destructive/20 p-4 text-sm text-destructive" role="alert">{error}</div>
	{/if}


	<Card.Root>
		<Card.Content class="p-4 sm:p-6">
			<DistributionBaseForm mode="create" onSubmit={handleSubmit} {submitting} />
		</Card.Content>
	</Card.Root>
</div>
