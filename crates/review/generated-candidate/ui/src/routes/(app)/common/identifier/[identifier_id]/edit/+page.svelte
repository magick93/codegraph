<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Edit page for Identifier. -->
<script lang="ts">
	import { goto } from '$app/navigation';

	import IdentifierForm from '$lib/components/forms/IdentifierForm.svelte';
	import { updateIdentifier } from '$lib/stores/common_identifier';
	import { toast } from 'svelte-sonner';
	import * as Card from '$lib/components/ui/card/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { IdentifierResponse, UpdateIdentifierRequest } from '$lib/api/types';


	const basePath = '/common/identifier';


	interface Props {
		data: {
			item: IdentifierResponse;
		};
	}

	let { data }: Props = $props();
	let submitting = $state(false);
	let error = $state('');

	async function handleSubmit(formData: UpdateIdentifierRequest) {
		submitting = true;
		error = '';
		try {

			await updateIdentifier(data.item.id, formData);

			toast.success(m.common_entity_updated({ entity: 'Identifier' }));
			goto(`${basePath}/${data.item.id}`);
		} catch (e) {
			error = (e as Error).message;
			toast.error(m.common_update_failed({ entity: 'Identifier' }));
		} finally {
			submitting = false;
		}
	}
</script>

<div class="space-y-6">
	<div>
		<a href={`${basePath}/${data.item.id}`} class="text-sm text-muted-foreground hover:text-foreground hover:underline">
			&larr; {m.common_back_to_detail()}
		</a>
		<h2 class="mt-1 text-xl font-semibold">{m.common_edit()} {m.common_identifier_title()}</h2>
	</div>


	{#if error}
		<div class="rounded-md bg-destructive/10 border border-destructive/20 p-4 text-sm text-destructive" role="alert">{error}</div>
	{/if}


	<Card.Root>
		<Card.Content class="p-4 sm:p-6">
			<IdentifierForm mode="edit" initialData={data.item} onSubmit={handleSubmit} {submitting} />
		</Card.Content>
	</Card.Root>
</div>
