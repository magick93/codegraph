<!-- Generated by codegraph. DO NOT EDIT. -->
<!-- Create page for Candidate. -->
<script lang="ts">
	import { goto } from '$app/navigation';

	import { page } from '$app/state';

	import CandidateForm from '$lib/components/forms/CandidateForm.svelte';
	import { createCandidate } from '$lib/stores/recruiting_candidate';
	import { toast } from 'svelte-sonner';
	import * as Card from '$lib/components/ui/card/index.js';
	import * as m from '$lib/paraglide/messages.js';
	import type { CreateCandidateRequest } from '$lib/api/types';


	const grandparentId = $derived(page.params.candidate_id);
	const parentId = $derived(page.params.application_id);
	const basePath = $derived(`/recruiting/candidate/${grandparentId}/application/${parentId}/candidate`);


	let submitting = $state(false);
	let error = $state('');

	async function handleSubmit(formData: CreateCandidateRequest) {
		submitting = true;
		error = '';
		try {

			const result = await createCandidate(grandparentId, parentId, formData);

			toast.success(m.common_entity_created({ entity: 'Candidate' }));
			goto(`${basePath}/${result.id}`);
		} catch (e) {
			error = (e as Error).message;
			toast.error(m.common_create_failed({ entity: 'Candidate' }));
		} finally {
			submitting = false;
		}
	}
</script>

<div class="space-y-6">
	<div>
		<a href={basePath} class="text-sm text-muted-foreground hover:text-foreground hover:underline">
			&larr; {m.common_back_to_list()}
		</a>
		<h2 class="mt-1 text-xl font-semibold">{m.recruiting_candidate_new()}</h2>
	</div>


	{#if error}
		<div class="rounded-md bg-destructive/10 border border-destructive/20 p-4 text-sm text-destructive" role="alert">{error}</div>
	{/if}


	<Card.Root>
		<Card.Content class="p-4 sm:p-6">
			<CandidateForm mode="create" onSubmit={handleSubmit} {submitting} />
		</Card.Content>
	</Card.Root>
</div>
