syntax = "proto3";
package common;

import "google/protobuf/timestamp.proto";

import "shared.proto";
option go_package = "common/;common";




/// CurrencyCodeList entity
message CurrencyCodeList {
  
    // Unique identifier (UUID)
  string id = 1;
  
    // Creation timestamp
  optional google.protobuf.Timestamp created_at = 998;
  
    // Last update timestamp
  optional google.protobuf.Timestamp updated_at = 999;
  
}

/// Create request for CurrencyCodeList
message CreateCurrencyCodeListRequest {
  
}

/// Update request for CurrencyCodeList
message UpdateCurrencyCodeListRequest {
  
    // Unique identifier (UUID)
  string id = 1;
  
}

/// Get request for CurrencyCodeList
message GetCurrencyCodeListRequest {
  
    // Entity UUID
  string id = 1;
  
}

/// Delete request for CurrencyCodeList
message DeleteCurrencyCodeListRequest {
  
    // Entity UUID
  string id = 1;
  
}

/// List request for CurrencyCodeList
message ListCurrencyCodeListRequest {
  
    // Number of items per page
  optional int32 page_size = 1;
  
    // Pagination token
  optional string page_token = 2;
  
    // Additional field filters
  repeated FilterClause filters = 5;
  
}

/// List response for CurrencyCodeList
message ListCurrencyCodeListResponse {
  
  
  repeated CurrencyCodeList data = 1;
  
    // Total number of items
  int32 total = 2;
  
    // Pagination token for next page
  optional string next_page_token = 3;
  
}



// ── CurrencyCodeList Service ──────────────────────────────────────────
service CurrencyCodeListService {
  
  // Create a new entity
  rpc Create(CreateCurrencyCodeListRequest) returns (CurrencyCodeList);
  
  // Get an entity by ID
  rpc Get(GetCurrencyCodeListRequest) returns (CurrencyCodeList);
  
  // Update an existing entity
  rpc Update(UpdateCurrencyCodeListRequest) returns (CurrencyCodeList);
  
  // Delete an entity by ID
  rpc Delete(DeleteCurrencyCodeListRequest) returns (google.protobuf.Empty);
  
  // List entities with pagination
  rpc List(ListCurrencyCodeListRequest) returns (ListCurrencyCodeListResponse);
  
}
