-- Full-text search infrastructure for common.distribution_base
-- Generated by codegraph. DO NOT EDIT.


-- Add tsvector column for full-text search
ALTER TABLE common.distribution_base
    ADD COLUMN IF NOT EXISTS search_tsv TSVECTOR;

-- GIN index for fast tsquery matching
CREATE INDEX IF NOT EXISTS idx_distribution_base_search_tsv
    ON common.distribution_base
    USING GIN (search_tsv);

-- Trigger function: maintains tsvector from source columns with weights
CREATE OR REPLACE FUNCTION common.update_distribution_base_search_tsv()
RETURNS TRIGGER AS $$
BEGIN
    NEW.search_tsv :=

        setweight(to_tsvector('english', COALESCE(NEW.description, '')), 'D')

    ;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_distribution_base_search_tsv
    BEFORE INSERT OR UPDATE ON common.distribution_base
    FOR EACH ROW
    EXECUTE FUNCTION common.update_distribution_base_search_tsv();

-- Backfill existing rows (safe no-op on empty tables)
UPDATE common.distribution_base
SET search_tsv =

    setweight(to_tsvector('english', COALESCE(description, '')), 'D')

;

